---
title: "2026-03-09 代码审核"
description: Notes from the 2026-03-09 Telegram code review and the proposed consolidation plan.
summary: "Unify Telegram DM routing and reply target handling across message, native command, and reaction paths."
read_when:
  - Addressing the 2026-03-09 Telegram review findings
  - Unifying Telegram DM session routing
  - Adding external_reply media parity
---

# 2026-03-09 代码审核

## 状态

- Telegram 范围已实施。
- 相关代码已独立提交：`c4773f543` (`Telegram: unify DM routing and reply context`)。
- 当前文档保留原始审查结论，同时补充实际落地结果与验证范围。
- 更大范围的仓库级改动仍需单独审查，不在这份提交内。
- plugin-sdk / runtime 第一批已完成并独立提交：
  - `24ea45684` (`Plugins: split runtime and scoped plugin-sdk surfaces`)
  - `696220027` (`Docs: update 2026-03-09 code review log`)
  - `33026c795` (`Plugins: add scoped plugin-sdk import guard`)
- gateway auth / control UI 第二批已完成并独立提交：
  - `2d48b277b` (`Gateway: harden auth and control-ui boundaries`)
  - `d785700fa` (`Docs: update 2026-03-09 code review log`)

## 背景

2026-03-09 的代码审核聚焦在 Telegram 入站链路的一致性问题。当前实现里，普通消息、native command、reaction、reply media 并没有完全共享同一套路由和上下文规则，导致在特定 Telegram 交付场景下，同一个用户的会话可能被拆成多个 session，或者 reply 引用已经被识别但媒体上下文没有真正带入。

本轮确认的 3 个问题如下：

1. `src/telegram/bot-native-commands.ts`
   - native command 的 DM 路由仍然直接使用 `String(chatId)`。
   - 普通消息路径已经改为优先使用 `senderId`。
   - 在 Telegram business / bridge 等 `chat.id != sender.id` 的场景下，slash command 会命中不同的 route/session。

2. `src/telegram/bot-handlers.ts`
   - reaction 事件在 DM 下仍然直接使用 `String(chatId)`。
   - 这会让 reaction 和普通消息进入不同 session。

3. `src/telegram/bot-handlers.ts`
   - `hasReplyTargetMedia()` 已经把 `reply_to_message ?? external_reply` 视为 reply target。
   - `resolveReplyMediaForMessage()` 仍然只读取 `reply_to_message`。
   - 结果是外部引用回复可以通过“有回复媒体”的判断，但拿不到真实的 `replyMedia`。

## 当前实现观察

- `src/telegram/bot-message-context.ts`
  - 普通消息在 DM 下已经使用 `resolveTelegramDirectPeerId({ chatId, senderId })`。
- `src/telegram/bot-native-commands.ts`
  - `resolveCommandRuntimeContext()` 仍然在 DM 下使用 `String(chatId)`。
- `src/telegram/bot-handlers.ts`
  - reaction 路径仍然在 DM 下使用 `String(chatId)`。
  - `hasReplyTargetMedia()` 和 `resolveReplyMediaForMessage()` 对 reply target 的定义不一致。
- `src/telegram/bot/helpers.ts`
  - 已经存在 `resolveTelegramDirectPeerId()`、`buildTelegramGroupPeerId()`、`describeReplyTarget()` 等基础 helper。
  - 这里适合继续收口共享路由和 reply target 解析逻辑。

## 目标

- 统一 Telegram DM 的路由主键，使普通消息、native command、reaction 命中同一 session。
- 统一 Telegram reply target 的解析，使 `reply_to_message` 和 `external_reply` 在文本与媒体上下文上保持一致。
- 用共享 helper 消除分叉实现，降低后续回归风险。
- 为上述行为补充稳定的回归测试。

## 非目标

- 不调整 callback / inline button 的授权语义。
- 不重做 Telegram forum topic 或 DM topic 的整体 session 设计。
- 不修改 ACP 绑定策略。
- 不引入配置 schema 变化或存储迁移。
- 不在这一轮扩展到 album 级别的 reply media 还原。

## 建议统一的语义

### 1. DM 路由

- DM `peerId` 规则：`senderId || chatId`
- 也就是优先使用真实发送者 id；只有缺失时才回退到 `chatId`

### 2. 群组路由

- group `peerId` 规则：`chatId[:topic]`
- forum topic 继续沿用 `buildTelegramGroupPeerId(chatId, resolvedThreadId)`

### 3. 群组父级 peer

- 只有 forum topic 场景继续保留 base `chatId` 作为 `parentPeer`
- 本轮不改变现有父级 peer 继承逻辑

### 4. Reply target

- reply target 统一定义为：`reply_to_message ?? external_reply`
- 文本引用、媒体判断、媒体提取都必须基于同一个 resolved target

### 5. `external_reply` 支持范围

- 第一轮只做到与当前 `resolveInboundMediaFileId()` 能力一致
- 也就是先保证单条媒体消息行为正确，不扩展到更复杂的多媒体集合恢复

## 技术方案

### 方案总览

本轮不建议只在 3 个点打补丁，而是建议抽出共享 helper，然后替换普通消息、native command、reaction、reply media 路径的分叉逻辑。这样可以把“路由主键规则”和“reply target 规则”收口成单一事实来源。

### 1. 在 `src/telegram/bot/helpers.ts` 增加共享 helper

建议新增两个高层 helper：

- `resolveTelegramRoutingPeerId(params)`
  - 输入建议包含：
    - `isGroup`
    - `chatId`
    - `resolvedThreadId`
    - `senderId`
  - 规则：
    - group: `buildTelegramGroupPeerId(chatId, resolvedThreadId)`
    - DM: `resolveTelegramDirectPeerId({ chatId, senderId })`

- `resolveTelegramReplyTargetMessage(msg)`
  - 返回：
    - `msg.reply_to_message ?? msg.external_reply`

建议保留现有 `resolveTelegramDirectPeerId()` 和 `buildTelegramGroupPeerId()`，把它们作为更低层的组合部件，而不是直接替换掉。

### 2. 调整 `src/telegram/bot-message-context.ts`

`buildTelegramMessageContext()` 当前在行为上已经是正确的，但仍然建议改为调用 `resolveTelegramRoutingPeerId()`，原因如下：

- 让普通消息路径成为共享实现的一部分，而不是继续保留内联逻辑。
- 后续如果 Telegram DM 路由规则再调整，只改 helper 即可。
- 可以明确 `bot-message-context.ts` 依赖的不是“拼接规则”，而是“统一路由约定”。

这一步应保持行为不变，不触碰：

- `threadSpec`
- `threadIdForConfig`
- `parentPeer`
- topic override 的 route/sessionKey 重算逻辑

### 3. 调整 `src/telegram/bot-native-commands.ts`

`resolveCommandRuntimeContext()` 需要改为使用共享路由 helper，并传入 `msg.from?.id` 作为 DM sender 候选值。

本轮只建议改这一层：

- `peerId`
- 由 `peerId` 派生的 route / configured route / session key 结果

本轮不建议扩大到：

- native command 的授权语义
- `parentConversationId`
- 其他与绑定/ACP 相关的行为

### 4. 调整 `src/telegram/bot-handlers.ts`

这里建议分为两部分：

#### reaction 路由统一

- reaction 路径改为调用 `resolveTelegramRoutingPeerId()`
- 在 DM 下与普通消息、native command 保持同一主键

保留当前边界：

- reaction 仍然只做 chat-level forum 处理
- 不尝试在本轮补齐 Telegram API 没有提供的 `message_thread_id` 信息

#### reply target 统一

- `hasReplyTargetMedia()` 改为调用 `resolveTelegramReplyTargetMessage()`
- `resolveReplyMediaForMessage()` 也改为调用 `resolveTelegramReplyTargetMessage()`
- 这样“是否有 reply 媒体”和“真正解析 reply 媒体”就使用同一份 reply target

### 5. 测试方案

本轮建议补 3 组回归测试：

- native command 路由一致性
  - 在 `chat.id != from.id` 的 DM 场景下
  - 断言 native command 与普通消息命中同一 route/session

- reaction 路由一致性
  - 在同样的 DM 场景下
  - 断言 reaction 事件进入和普通消息一致的 sessionKey

- `external_reply` 媒体一致性
  - 构造带媒体的 `external_reply`
  - 断言 `replyMedia` 能真正进入消息处理链路，而不仅仅是通过 gating

建议优先补在这些测试文件中：

- `src/telegram/bot-native-commands.test.ts`
- `src/telegram/bot.test.ts`
- `src/telegram/bot/helpers.test.ts`

如果共享 helper 的抽象影响到既有 DM 线程行为，也可以补充或复用：

- `src/telegram/bot-message-context.dm-threads.test.ts`

## 建议执行顺序

1. 在 `src/telegram/bot/helpers.ts` 新增共享 helper。
2. 将 `src/telegram/bot-message-context.ts` 切换到共享 helper，确保基线行为不变。
3. 修改 `src/telegram/bot-native-commands.ts` 和 `src/telegram/bot-handlers.ts` 的分叉逻辑。
4. 补齐 `native command`、`reaction`、`external_reply media` 三类回归测试。
5. 跑 Telegram 相关测试，再跑一次完整测试或至少相关回归集合。

## 实施结果

本轮实际落地内容如下：

- `src/telegram/bot/helpers.ts`
  - 新增 `resolveTelegramRoutingPeerId()`
  - 新增 `resolveTelegramReplyTargetMessage()`
  - `describeReplyTarget()` 改为复用统一 reply target helper

- `src/telegram/bot-message-context.ts`
  - 普通消息路由改为使用 `resolveTelegramRoutingPeerId()`
  - 行为目标是“统一实现”，不是改变既有正确语义

- `src/telegram/bot-native-commands.ts`
  - native command 的 DM 路由改为优先使用 `senderId`

- `src/telegram/bot-handlers.ts`
  - reaction 的 DM 路由改为优先使用 `senderId`
  - `hasReplyTargetMedia()` 与 `resolveReplyMediaForMessage()` 改为共享 reply target 解析
  - `resolveTelegramSessionState()` 也切换到同一套 DM 路由 helper
    - 这是实施时补进的同类遗漏
    - 它属于 session 路由一致性问题，不涉及 callback 授权语义调整

- 测试
  - `src/telegram/bot/helpers.test.ts`
  - `src/telegram/bot-native-commands.session-meta.test.ts`
  - `src/telegram/bot.test.ts`
  - 已补覆盖：
    - DM sender-based 路由 helper
    - native command 命中 sender-scoped session
    - reaction 命中 sender-scoped session
    - `external_reply` 图片媒体进入 inbound context

## 本轮验证

本轮已通过的验证包括：

- `pnpm test -- src/telegram/bot/helpers.test.ts src/telegram/bot-native-commands.session-meta.test.ts src/telegram/bot-message-context.dm-threads.test.ts`
- `pnpm test -- src/telegram/bot.test.ts -t "includes external reply image media|routes direct-message reactions by sender id"`

说明：

- 这些验证覆盖了本轮直接改动的 helper、native command、DM session、reaction、`external_reply` media 主链路。
- 更宽范围的 Telegram 测试文件仍存在仓库当前变更集带来的独立问题，需要在后续仓库级审查中处理。

## 验收标准

- 普通消息、native command、reaction 在 DM 下使用相同的会话主键规则。
- `chat.id != sender.id` 的 Telegram business / bridge 场景下，不再出现 session 分裂。
- `external_reply` 带媒体时，`replyMedia` 能进入下游处理链路。
- 普通 `reply_to_message` 行为不回退。
- 既有 forum topic 和 group session 路由行为不发生意外变化。

## 待决策项

### 1. 修复方式

待确认：

- 采用“3 个调用点最小修补”
- 或采用“共享 helper 收口”

建议：

- 采用“共享 helper 收口”

原因：

- 当前问题本质上是规则分叉，而不是单点逻辑错误。
- 如果继续只修调用点，后续新增入口仍然容易再次偏离。

### 2. `external_reply` 媒体支持深度

待确认：

- 只做与当前单消息媒体提取能力对齐
- 或进一步扩到更复杂的引用媒体恢复

建议：

- 第一轮只做单消息媒体对齐

原因：

- 可以直接修复本轮回归。
- 风险和验证范围更可控。

### 3. 是否顺手处理 callback / inline button 语义

待确认：

- 本轮只修路由与 reply target
- 或顺手把 callback 相关 allowlist 语义一起整理

建议：

- 本轮不要顺手处理

原因：

- callback/allowlist 是相邻但不同类的问题。
- 混入同一 patch 会扩大行为面和测试面。

## 风险说明

- 如果共享 helper 的命名或职责边界不清，可能把低层工具函数和高层路由约定混在一起，反而增加维护成本。
- 如果测试只覆盖 helper，不覆盖实际消息处理链路，可能仍然漏掉 route/session 级别的回归。
- 如果本轮顺手修改 callback 授权语义，容易让 patch 目标失焦，增加 reviewer 判断成本。

## 结论

Telegram 范围已经按“共享 helper 收口 + 小范围调用点替换 + 回归测试补齐”的方式完成实施。该方案在不扩大配置和授权语义范围的前提下，修复了 Telegram DM 路由分叉和 `external_reply` 媒体上下文缺失的问题，并补齐了相应回归测试。仓库其余大范围改动需要作为下一阶段单独审查。

## 下一阶段：plugin-sdk / runtime 首批改造

### 目标

下一阶段的第一批改造，不是继续做 channel 行为修补，而是收口插件边界本身。目标分为 3 条：

- 将 `src/plugins/runtime` 从单一大实现和大类型定义，拆成可维护的内部模块。
- 将 `openclaw/plugin-sdk` 从单一大出口拆成分层公开面，同时保留对旧插件的兼容入口。
- 只纳入与插件边界治理直接相关的通用能力补全，不混入命令授权和 channel 行为变化。

### 现状复核

本轮在进入实施前，已经重新核对过这批改动的关键同步点：

- 运行时拆分：
  - `src/plugins/runtime/index.ts`
  - `src/plugins/runtime/types.ts`
  - `src/plugins/runtime/types-core.ts`
  - `src/plugins/runtime/types-channel.ts`
  - `src/plugins/runtime/runtime-*.ts`
- plugin-sdk 公开面：
  - `src/plugin-sdk/index.ts`
  - `src/plugin-sdk/core.ts`
  - `src/plugin-sdk/compat.ts`
  - `src/plugin-sdk/<channel-or-feature>.ts`
- 构建与加载：
  - `package.json`
  - `tsdown.config.ts`
  - `scripts/write-plugin-sdk-entry-dts.ts`
  - `scripts/copy-plugin-sdk-root-alias.mjs`
  - `src/plugins/loader.ts`
  - `src/plugin-sdk/root-alias.cjs`
- 约束与测试：
  - `src/plugin-sdk/index.test.ts`
  - `src/plugin-sdk/subpaths.test.ts`
  - `src/plugin-sdk/root-alias.test.ts`
  - `src/plugins/runtime/index.test.ts`
  - `src/plugins/runtime/types.contract.test.ts`
  - `scripts/check-no-monolithic-plugin-sdk-entry-imports.ts`

已确认通过的定向验证：

- `pnpm test -- src/plugin-sdk/index.test.ts src/plugin-sdk/subpaths.test.ts src/plugin-sdk/root-alias.test.ts src/plugins/runtime/index.test.ts src/plugins/runtime/types.contract.test.ts`
- `pnpm lint:plugins:no-monolithic-plugin-sdk-entry-imports`

### 需要补进方案的关键约束

这批在正式实施前，额外补进 4 条约束：

1. 结构治理与行为变更必须拆开
   - 本批只处理 `plugin-sdk` / `runtime` 的边界治理。
   - 不把命令授权、插件命令行为、Slack action 行为等语义改动混进来。

2. plugin-sdk 子路径清单必须收口
   - 当前同一份子路径清单分散在：
     - `package.json`
     - `tsdown.config.ts`
     - `scripts/write-plugin-sdk-entry-dts.ts`
     - `src/plugins/loader.ts`
     - `src/plugin-sdk/subpaths.test.ts`
   - 这一批必须降低这些同步点的漂移风险。

3. `package.json` 必须严格限 scope
   - 只允许保留与 plugin-sdk 子路径导出、构建接线、必要 lint/test 接线直接相关的修改。
   - 不应把版本号变更、依赖升级、无关脚本新增与本批捆绑。

4. 公开入口语义需要定死
   - `openclaw/plugin-sdk`
     - 旧插件兼容入口。
   - `openclaw/plugin-sdk/core`
     - 窄而稳定的基础入口。
   - `openclaw/plugin-sdk/compat`
     - 为需要较宽内部能力的现有插件保留的兼容入口。
   - `openclaw/plugin-sdk/<channel-or-feature>`
     - 面向 channel/plugin 的专用窄入口。

### 批次内再拆分

第一批正式实施时，进一步拆为 3 个连续小批次：

#### 1. runtime 内部拆分

范围：

- `src/plugins/runtime/index.ts`
- `src/plugins/runtime/types.ts`
- `src/plugins/runtime/types-core.ts`
- `src/plugins/runtime/types-channel.ts`
- `src/plugins/runtime/runtime-*.ts`
- 对应 runtime tests

目标：

- 纯结构重组。
- 不改变 `createPluginRuntime()` 对外暴露的运行时形状。

#### 2. plugin-sdk 公开面与构建/加载接线

范围：

- `src/plugin-sdk/index.ts`
- `src/plugin-sdk/core.ts`
- `src/plugin-sdk/compat.ts`
- `src/plugin-sdk/<channel-or-feature>.ts`
- `package.json` 中与 plugin-sdk exports 直接相关的部分
- `tsdown.config.ts`
- `scripts/write-plugin-sdk-entry-dts.ts`
- `scripts/copy-plugin-sdk-root-alias.mjs`
- `src/plugins/loader.ts`
- `src/plugin-sdk/root-alias.cjs`
- 对应 subpath / root-alias tests

目标：

- 新子路径入口在 src / dist 两种运行形态下都可解析。
- 旧入口兼容不回退。
- plugin loader 的 alias 逻辑与 package exports、构建产物保持一致。

#### 3. 通用 SDK 能力补全

建议纳入本批的通用能力文件：

- `src/plugin-sdk/webhook-targets.ts`
- `src/plugin-sdk/status-helpers.ts`
- `src/plugin-sdk/persistent-dedupe.ts`
- `src/plugin-sdk/json-store.ts`
- `src/plugin-sdk/account-id.ts`

这部分属于插件基础设施补全，与插件边界治理主题一致。

### 明确排除项

以下内容虽然出现在相邻目录中，但不纳入这一批：

- 命令授权 / 插件命令行为
  - `src/plugin-sdk/command-auth.ts`
  - `src/plugins/commands.ts`
- channel 行为改动
  - `src/plugin-sdk/slack-message-actions.ts`
  - `src/plugin-sdk/tool-send.ts`

这些内容需要后移到 auth / command 或 channel 批次处理，避免第一批 scope 漂移。

### 子路径清单同步策略

这批已确认采用的策略是：

- 继续保留 `package.json` 的显式 exports。
- 引入单一来源的 plugin-sdk 子路径清单，供构建、loader、测试复用。
- 优先采用“单一清单 + 校验”的方式，而不是全自动重写 `package.json`。

采用该策略的原因：

- 风险比自动改写 `package.json` 更低。
- 更容易逐步接入现有 build / test / loader 逻辑。
- 可以先解决清单漂移问题，再决定是否需要进一步自动生成。

### 实施门槛

第一批开始实施时，最少需要保留这些验证门槛：

- `pnpm lint:plugins:no-monolithic-plugin-sdk-entry-imports`
- `pnpm test -- src/plugin-sdk/index.test.ts src/plugin-sdk/subpaths.test.ts src/plugin-sdk/root-alias.test.ts src/plugins/runtime/index.test.ts src/plugins/runtime/types.contract.test.ts`
- `pnpm build:strict-smoke` 或同等强度的 plugin-sdk 构建校验

### 当前决策

对于 plugin-sdk / runtime 第一批，当前已确认：

- 按 3 个连续小批次推进，而不是把所有相邻改动打成一个 patch。
- 使用“单一清单 + 校验”的方式收口子路径同步点。
- `openclaw/plugin-sdk` 旧入口本轮继续保持兼容，不额外增加 deprecation 警告。

### 当前实施进度

截至当前窗口上下文压缩前，第一批已经完成的代码工作如下：

- 新增共享清单：
  - `src/plugin-sdk/export-manifest.ts`
- 已切换到共享清单的同步点：
  - `tsdown.config.ts`
  - `scripts/write-plugin-sdk-entry-dts.ts`
  - `src/plugins/loader.ts`
  - `src/plugin-sdk/subpaths.test.ts`
  - `src/plugin-sdk/root-alias.test.ts`
  - `src/plugins/loader.test.ts`
  - `src/plugins/runtime/index.test.ts`
- 已补上的校验：
  - `src/plugin-sdk/export-manifest.test.ts`
    - 新增共享清单 packageSubpath / entryName 唯一性回归
    - 新增 scoped entries 与完整 manifest 对齐回归
    - 新增 package exports 映射与 manifest entries 对齐回归
  - `src/plugin-sdk/subpaths.test.ts`
    - 新增 `package.json` plugin-sdk exports 与共享清单一致性的断言
    - 新增 `account-id` 与 `keyed-async-queue` 直接子路径导出断言
    - 新增 `core` 窄入口边界回归，确保其不泄漏 `resolveControlCommandGate`、`resolveSenderCommandAuthorization`、`handleSlackMessageAction`、`extractToolSend`
  - `src/plugin-sdk/channel-lifecycle.test.ts`
    - 已覆盖 `waitUntilAbort` 与 `keepHttpServerTaskAlive` 的直接行为回归
  - `src/plugin-sdk/webhook-memory-guards.test.ts`
    - 已覆盖 fixed-window limiter、bounded counter、anomaly tracker 与共享默认值回归
  - `src/plugin-sdk/webhook-request-guards.test.ts`
    - 已覆盖基础 method/json 限制、body 读取、in-flight limiter 等直接行为回归
  - `src/plugins/loader.test.ts`
    - 新增 scoped plugin-sdk alias map 的回归测试
    - 回归覆盖已扩到 `compat`、`core`、`account-id`、`telegram` 四类子路径
  - `src/plugin-sdk/root-alias.test.ts`
    - 新增根入口快路径代理 surface 限定回归
    - 新增快路径 `emptyPluginConfigSchema` 与兼容入口实现对齐回归
    - 新增快路径 `resolveControlCommandGate` 与兼容入口实现对齐回归
  - `src/plugins/runtime/index.test.ts`
    - 新增默认 subagent 不可用行为与注入 subagent 覆盖行为的回归测试
    - 新增顶层 runtime 命名空间形状稳定性回归
  - `src/plugin-sdk/status-helpers.test.ts`
    - 新增 `buildProbeChannelStatusSummary()` 的直接回归测试
  - `src/plugin-sdk/json-store.test.ts`
    - 新增缺失文件回退、损坏 JSON 回退、原子写入回读的直接回归测试
  - `src/plugin-sdk/index.test.ts`
    - 新增第一批 plugin infrastructure helpers 的根入口导出回归测试
    - 根入口回归已扩到 `normalizeAccountId`、`readJsonFileWithFallback`、`writeJsonFileAtomically`、`createPersistentDedupe`

截至当前已确认通过的验证：

- `pnpm test -- src/plugin-sdk/index.test.ts src/plugin-sdk/subpaths.test.ts src/plugin-sdk/root-alias.test.ts src/plugins/runtime/index.test.ts src/plugins/runtime/types.contract.test.ts`
- `pnpm test -- src/plugin-sdk/status-helpers.test.ts src/plugin-sdk/webhook-targets.test.ts src/plugin-sdk/persistent-dedupe.test.ts src/plugin-sdk/keyed-async-queue.test.ts`
- `pnpm test -- src/plugins/loader.test.ts -t "plugin-sdk|root-alias|scoped plugin-sdk aliases"`
- `pnpm test -- src/plugins/runtime/index.test.ts src/plugins/runtime/types.contract.test.ts src/plugins/runtime/gateway-request-scope.test.ts`
- `pnpm test -- src/plugin-sdk/index.test.ts src/plugin-sdk/subpaths.test.ts src/plugin-sdk/root-alias.test.ts`
- `pnpm test -- src/plugin-sdk/subpaths.test.ts src/plugin-sdk/index.test.ts`
- `pnpm test -- src/plugin-sdk/subpaths.test.ts src/plugin-sdk/index.test.ts src/plugins/runtime/index.test.ts`
- `pnpm test -- src/plugin-sdk/root-alias.test.ts src/plugin-sdk/subpaths.test.ts`
- `pnpm test -- src/plugin-sdk/root-alias.test.ts`
- `pnpm test -- src/plugin-sdk/json-store.test.ts src/plugin-sdk/index.test.ts`
- `pnpm test -- src/plugin-sdk/export-manifest.test.ts src/plugin-sdk/subpaths.test.ts`
- `pnpm test -- src/plugin-sdk/channel-lifecycle.test.ts src/plugin-sdk/webhook-request-guards.test.ts`
- `pnpm test -- src/plugin-sdk/index.test.ts src/plugin-sdk/webhook-memory-guards.test.ts`
- `pnpm test -- src/plugin-sdk/export-manifest.test.ts src/plugin-sdk/index.test.ts src/plugin-sdk/subpaths.test.ts src/plugin-sdk/root-alias.test.ts src/plugin-sdk/json-store.test.ts src/plugin-sdk/status-helpers.test.ts src/plugin-sdk/webhook-targets.test.ts src/plugin-sdk/persistent-dedupe.test.ts src/plugin-sdk/keyed-async-queue.test.ts src/plugins/runtime/index.test.ts src/plugins/runtime/types.contract.test.ts src/plugins/runtime/gateway-request-scope.test.ts`
- `pnpm test -- src/plugin-sdk/channel-lifecycle.test.ts src/plugin-sdk/export-manifest.test.ts src/plugin-sdk/index.test.ts src/plugin-sdk/json-store.test.ts src/plugin-sdk/keyed-async-queue.test.ts src/plugin-sdk/persistent-dedupe.test.ts src/plugin-sdk/root-alias.test.ts src/plugin-sdk/status-helpers.test.ts src/plugin-sdk/subpaths.test.ts src/plugin-sdk/webhook-memory-guards.test.ts src/plugin-sdk/webhook-request-guards.test.ts src/plugin-sdk/webhook-targets.test.ts src/plugins/runtime/index.test.ts src/plugins/runtime/types.contract.test.ts src/plugins/runtime/gateway-request-scope.test.ts`
- `pnpm test -- src/plugin-sdk/status-helpers.test.ts`
- `pnpm test -- src/plugins/loader.test.ts -t "plugin-sdk|root-alias|scoped plugin-sdk aliases"`
- `pnpm lint:plugins:no-monolithic-plugin-sdk-entry-imports`
- `pnpm build:strict-smoke`
  - 仅存在仓库当前已有的 `[INEFFECTIVE_DYNAMIC_IMPORT]` 警告
  - 未引入新的 plugin-sdk / runtime 构建错误

当前结论：

- batch 1 的共享清单、公开入口、根入口代理、loader alias、runtime 入口、通用 helper 以及对应测试边界已经收口完成。
- 后续工作应转入“独立提交整理”，继续保持不混入 `command-auth`、`plugins/commands`、`slack-message-actions`、`tool-send` 等行为改动。

### 独立提交整理补充验证

在进入独立提交整理时，又补跑了一轮更宽的 `plugin-sdk / runtime` 聚合验证，用来确认相邻的公开 helper 与命令/动作辅助模块不会在提交拆分时留下未覆盖区域。

已额外通过：

- `pnpm test -- src/plugin-sdk/command-auth.test.ts src/plugin-sdk/slack-message-actions.test.ts src/plugin-sdk/tool-send.test.ts src/plugins/commands.test.ts`
- `pnpm test -- src/plugin-sdk/*.test.ts src/plugins/runtime/*.test.ts src/plugins/commands.test.ts src/plugins/loader.test.ts`

补充说明：

- `tool-send` 已新增直接回归，覆盖 `threadId` 解析与空值归一化。
- 独立提交整理阶段确认 `command-auth`、`slack-message-actions`、`tool-send`、`plugins/commands` 都已有独立测试兜底。

### 当前第一批边界候选

截至当前，第一批已经明确吸收的方向包括：

- plugin-sdk 子路径清单与构建接线
  - `src/plugin-sdk/index.ts`
  - `src/plugin-sdk/core.ts`
  - `src/plugin-sdk/compat.ts`
  - `src/plugin-sdk/export-manifest.ts`
  - `src/plugin-sdk/root-alias.cjs`
  - `src/plugin-sdk/root-alias.test.ts`
  - `tsdown.config.ts`
  - `scripts/write-plugin-sdk-entry-dts.ts`
  - `src/plugins/loader.ts`
  - `src/plugin-sdk/subpaths.test.ts`
- runtime 入口与拆分后的基础回归
  - `src/plugins/runtime/index.ts`
  - `src/plugins/runtime/types.ts`
  - `src/plugins/runtime/index.test.ts`
  - `src/plugins/runtime/types.contract.test.ts`
  - `src/plugins/runtime/gateway-request-scope.test.ts`
  - 当前已额外锁定：
    - 顶层 runtime namespace 集合不漂移
    - `subagent` 公开方法集合不漂移
- 本批允许纳入的通用 helper
  - `src/plugin-sdk/channel-lifecycle.ts`
  - `src/plugin-sdk/webhook-memory-guards.ts`
  - `src/plugin-sdk/webhook-request-guards.ts`
  - `src/plugin-sdk/keyed-async-queue.ts`
  - `src/plugin-sdk/status-helpers.ts`
  - `src/plugin-sdk/webhook-targets.ts`
  - `src/plugin-sdk/persistent-dedupe.ts`
  - `src/plugin-sdk/json-store.ts`
  - `src/plugin-sdk/account-id.ts`
  - 当前状态说明：
    - `json-store` 已有直接行为回归
    - `account-id` 当前为 `routing/session-key` 的纯 re-export，子路径与根入口导出已被回归覆盖
    - 其余纳入的通用 helper 当前均已有直接行为测试或根入口 / 子路径导出回归

当前额外明确的边界约束：

- `openclaw/plugin-sdk/core`
  - 继续作为窄入口存在
  - 当前已通过回归锁定不导出：
    - `resolveControlCommandGate`
    - `resolveSenderCommandAuthorization`
    - `handleSlackMessageAction`
    - `extractToolSend`
- `openclaw/plugin-sdk`
  - 继续保留旧入口兼容语义
- `src/plugin-sdk/root-alias.cjs`
  - 继续保留快路径代理
  - 当前已通过回归锁定：
    - 在未触发 monolithic 加载前，仅暴露预期的快路径 surface
    - `emptyPluginConfigSchema` 快路径实现与 `compat` 导出保持一致
    - `resolveControlCommandGate` 快路径实现与 `compat` 导出保持一致
- `openclaw/plugin-sdk/compat`
  - 继续承担比 `core` 更宽的兼容公开面

截至当前，仍明确排除在第一批之外的相邻行为改动：

- `src/plugin-sdk/command-auth.ts`
- `src/plugins/commands.ts`
- `src/plugin-sdk/slack-message-actions.ts`
- `src/plugin-sdk/tool-send.ts`

这一轮继续推进时，应优先把“已吸收范围”和“明确排除范围”维持清晰，而不是再扩大第一批主题。

## 当前阶段：gateway auth / control UI 第二批改造

### 目标

第二批当前收口的主题是 gateway 的 HTTP / WS 鉴权边界，而不是更宽的 gateway 全量重构。当前已确认纳入的子目标包括：

- gateway auth mode 与 SecretRef 解析一致性
- Control UI root-mounted / basePath 路由与静态资源边界
- browser-origin / trusted-proxy / control-ui device identity 鉴权收口
- plugin HTTP route 与 gateway auth 的边界收口
- device-auth v3 payload 与 client/device token 存储辅助逻辑

### 当前已纳入范围

- gateway auth / control-ui 核心实现
  - `src/gateway/auth.ts`
  - `src/gateway/auth-install-policy.ts`
  - `src/gateway/auth-mode-policy.ts`
  - `src/gateway/control-ui.ts`
  - `src/gateway/control-ui-http-utils.ts`
  - `src/gateway/control-ui-routing.ts`
  - `src/gateway/control-ui-csp.ts`
  - `src/gateway/control-ui-contract.ts`
  - `src/gateway/origin-check.ts`
  - `src/gateway/probe-auth.ts`
  - `src/gateway/device-auth.ts`
  - `src/gateway/device-metadata-normalization.ts`
  - `src/gateway/startup-control-ui-origins.ts`
- gateway server HTTP / WS auth boundary
  - `src/gateway/server/http-auth.ts`
  - `src/gateway/server/http-listen.ts`
  - `src/gateway/server-http.ts`
  - `src/gateway/server/ws-connection/connect-policy.ts`
  - `src/gateway/server/ws-connection/message-handler.ts`
  - `src/gateway/server/plugins-http.ts`
  - `src/gateway/server/plugins-http/*`
  - `src/gateway/security-path.ts`
- gateway client / device auth token storage
  - `src/gateway/client.ts`
  - `src/infra/device-auth-store.ts`
  - `src/shared/device-auth-store.ts`
- 测试与 harness
  - `src/gateway/auth.test.ts`
  - `src/gateway/auth-mode-policy.test.ts`
  - `src/gateway/control-ui.http.test.ts`
  - `src/gateway/control-ui-routing.test.ts`
  - `src/gateway/control-ui-csp.test.ts`
  - `src/gateway/device-auth.test.ts`
  - `src/gateway/origin-check.test.ts`
  - `src/gateway/probe-auth.test.ts`
  - `src/gateway/server/http-listen.test.ts`
  - `src/gateway/server.auth.*`
  - `src/gateway/server.plugin-http-auth.test.ts`
  - `src/gateway/server/plugins-http.test.ts`
  - `src/gateway/server/ws-connection/connect-policy.test.ts`
  - `src/gateway/client.test.ts`
  - `src/shared/device-auth-store.test.ts`
  - `src/gateway/test-helpers.server.ts`
  - `src/gateway/test-helpers.e2e.ts`
  - `src/gateway/server-http.test-harness.ts`

### 本轮补进的额外修正

- 修复了 gateway 最小测试模式只识别 `VITEST === "1"` 的问题。
  - 当前仓库测试环境实际使用 `VITEST="true"`。
  - 这一处不修会导致 auth 套件误启动 `TaskService`，并在测试清理阶段出现后台 `tasks.json` 原子写盘的 `ENOENT` 异步错误。
  - 当前已在 `src/gateway/server.impl.ts` 中收口为同时接受 `"1"` 和 `"true"`。

- 为共享 device-auth store helper 增加了直接单元测试。
  - 当前已覆盖：
    - role/scopes 归一化写入
    - 按 deviceId 读取隔离
    - 指定 role token 清理

### 当前定向验证

当前这一批已通过的验证包括：

- `pnpm test -- src/gateway/auth.test.ts src/gateway/auth-mode-policy.test.ts src/gateway/device-auth.test.ts src/gateway/origin-check.test.ts src/gateway/probe-auth.test.ts src/gateway/control-ui-csp.test.ts src/gateway/control-ui.http.test.ts src/gateway/control-ui-routing.test.ts src/gateway/server/http-listen.test.ts src/gateway/server.auth.modes.test.ts src/gateway/server.auth.control-ui.test.ts src/gateway/server.auth.default-token.test.ts src/gateway/server.auth.browser-hardening.test.ts src/gateway/server.plugin-http-auth.test.ts`
- `pnpm test -- src/shared/device-auth-store.test.ts src/gateway/client.test.ts src/commands/doctor-gateway-auth-token.test.ts`
- `pnpm test -- src/gateway/server/ws-connection/connect-policy.test.ts src/gateway/server/plugins-http.test.ts`

### 当前结论

截至当前，gateway auth / control UI 第二批已经达到“范围可描述、测试可回归、可以进入独立提交整理”的状态。下一步应继续维持提交边界，不把 `hooks`、`openai/openresponses`、`gateway methods / sessions / agent` 等相邻主题混入同一提交链。

## 当前阶段：shared DM policy + WhatsApp web inbound 第三批改造

### 目标

第三批当前收口的主题是 shared DM/group allowlist 规则与 WhatsApp web inbound / auto-reply 行为一致性，而不是更宽的 Slack / Telegram / outbound / media 全量改造。当前已确认纳入的子目标包括：

- 把 DM/group allowlist、pairing-store、control-command gate 的通用决策逻辑收口到共享 helper
- 让 WhatsApp web inbound access-control、auto-reply command gate、main-session last-route 更新复用同一套 DM policy 规则
- 把 inbound monitor 的规范化、read receipt、消息 enrich、enqueue 过程拆成更清晰的内部步骤
- 收口 Web auto-reply 和 monitor 测试，减少重复零散用例

### 当前已纳入范围

- shared DM policy helper
  - `src/security/dm-policy-shared.ts`
  - `src/security/dm-policy-shared.test.ts`
  - `src/security/dm-policy-channel-smoke.test.ts`
- inbound session / envelope helper
  - `src/channels/session-envelope.ts`
- WhatsApp web inbound / auto-reply
  - `src/web/inbound/access-control.ts`
  - `src/web/inbound/access-control.test.ts`
  - `src/web/inbound/monitor.ts`
  - `src/web/inbound.types.ts`
  - `src/web/inbound.media.test.ts`
  - `src/web/auto-reply/monitor/process-message.ts`
  - `src/web/auto-reply/monitor/message-line.ts`
  - `src/web/auto-reply/monitor/process-message.inbound-contract.test.ts`
  - `src/web/auto-reply/deliver-reply.test.ts`
- Web monitor / auto-reply test harness 与测试整理
  - `src/web/monitor-inbox.test-harness.ts`
  - `src/web/monitor-inbox.allows-messages-from-senders-allowfrom-list.test.ts`
  - `src/web/monitor-inbox.blocks-messages-from-unauthorized-senders-not-allowfrom.test.ts`
  - `src/web/monitor-inbox.captures-media-path-image-messages.test.ts`
  - `src/web/test-helpers.ts`
  - `src/web/auto-reply.broadcast-groups.combined.test.ts`
  - `src/web/auto-reply.web-auto-reply.connection-and-logging.e2e.test.ts`
  - 删除被合并的旧测试：
    - `src/web/auto-reply.broadcast-groups.broadcasts-sequentially-configured-order.test.ts`
    - `src/web/auto-reply.broadcast-groups.skips-unknown-broadcast-agent-ids-agents-list.test.ts`
    - `src/web/auto-reply.typing-controller-idle.test.ts`
    - `src/web/auto-reply.web-auto-reply.monitor-logging.test.ts`
    - `src/web/auto-reply.web-auto-reply.reconnects-after-connection-close.test.ts`

### 本轮补进的额外收口

- group allowlist 现在明确不再继承 pairing-store 的 DM 配对结果。
  - pairing-store 继续只影响 DM allowlist
  - group auth 仅依赖显式 `groupAllowFrom` 或配置 fallback
- 新增 pinned main DM owner 推断 helper
  - 用于 `dmScope="main"` 时避免非 owner sender 覆写 main session 的 `lastRoute`
- 新增 inbound session envelope helper
  - 收口 `storePath`、`envelopeOptions`、`previousTimestamp`
  - 避免每个 channel 自己拼装 session envelope 上下文
- Web inbound monitor 现在显式透传 `fromMe`
  - 让 envelope / reply-prefix / inbound line 可以区分 owner 侧消息来源

### 当前明确排除范围

- `src/web/media.ts`
- `src/web/outbound.ts`
- Slack monitor / auth / slash / threading 相关改动
- `src/channels/plugins/outbound/**`
- 更宽的 routing / sessions / security 其他主题

### 当前定向验证

当前这一批已通过的验证包括：

- `pnpm test -- src/security/dm-policy-shared.test.ts src/security/dm-policy-channel-smoke.test.ts src/web/inbound/access-control.test.ts src/web/inbound.media.test.ts src/web/monitor-inbox.allows-messages-from-senders-allowfrom-list.test.ts src/web/monitor-inbox.blocks-messages-from-unauthorized-senders-not-allowfrom.test.ts src/web/monitor-inbox.captures-media-path-image-messages.test.ts src/web/auto-reply/deliver-reply.test.ts src/web/auto-reply/monitor/process-message.inbound-contract.test.ts src/web/auto-reply.broadcast-groups.combined.test.ts src/web/auto-reply.web-auto-reply.connection-and-logging.e2e.test.ts`
- `pnpm test -- src/web/monitor-inbox.streams-inbound-messages.test.ts`

### 当前结论

截至当前，shared DM policy + WhatsApp web inbound 第三批已经达到“范围可描述、共享逻辑已收口、测试可回归、可以进入独立提交整理”的状态。下一步应保持边界，不把 `src/web/media.ts`、`src/web/outbound.ts`、Slack monitor、channel outbound 等相邻主题混入同一批提交。

## 当前阶段：channel outbound adapter 第四批改造

### 目标

第四批当前收口的主题是 channel outbound adapter 的统一 payload 发送能力，而不是更宽的 plugin registry / setup migration / status surface 变更。当前已确认纳入的子目标包括：

- 为 direct/gateway outbound adapter 提供共享 `sendPayload` 路径
- 统一 text / single-media / multi-media payload 的发送语义
- 把 `cfg` 透传到具体 channel send 实现，避免 adapter 层丢失已解析配置
- 为 WhatsApp poll / Discord webhook / Slack send / Telegram thread send 补足回归

### 当前已纳入范围

- shared outbound helper
  - `src/channels/plugins/outbound/direct-text-media.ts`
- channel outbound adapter
  - `src/channels/plugins/outbound/discord.ts`
  - `src/channels/plugins/outbound/slack.ts`
  - `src/channels/plugins/outbound/telegram.ts`
  - `src/channels/plugins/outbound/whatsapp.ts`
  - `src/channels/plugins/outbound/imessage.ts`
  - `src/channels/plugins/outbound/signal.ts`
- WhatsApp gateway outbound bridge
  - `src/web/outbound.ts`
- 测试
  - `src/channels/plugins/outbound/direct-text-media.sendpayload.test.ts`
  - `src/channels/plugins/outbound/discord.sendpayload.test.ts`
  - `src/channels/plugins/outbound/slack.sendpayload.test.ts`
  - `src/channels/plugins/outbound/whatsapp.sendpayload.test.ts`
  - `src/channels/plugins/outbound/whatsapp.poll.test.ts`
  - `src/channels/plugins/outbound/discord.test.ts`
  - `src/channels/plugins/outbound/slack.test.ts`
  - `src/channels/plugins/outbound/telegram.test.ts`
  - `src/channels/plugins/plugins-channel.test.ts`
  - `src/web/outbound.test.ts`

### 当前明确排除范围

- `src/channels/plugins/setup-helpers.ts`
- `src/channels/plugins/index.ts`
- `src/channels/plugins/plugins-core.test.ts`
- `src/channels/plugins/types.adapters.ts`
- `src/channels/plugins/types.core.ts`
- `src/web/media.ts`
- 更宽的 channel action / onboarding / status / registry 主题

### 当前定向验证

当前这一批已通过的验证包括：

- `pnpm test -- src/channels/plugins/outbound/direct-text-media.sendpayload.test.ts src/channels/plugins/outbound/discord.sendpayload.test.ts src/channels/plugins/outbound/slack.sendpayload.test.ts src/channels/plugins/outbound/whatsapp.sendpayload.test.ts src/channels/plugins/outbound/whatsapp.poll.test.ts src/channels/plugins/outbound/discord.test.ts src/channels/plugins/outbound/slack.test.ts src/channels/plugins/outbound/telegram.test.ts`
- `pnpm test -- src/web/outbound.test.ts src/channels/plugins/plugins-channel.test.ts`

### 当前结论

截至当前，channel outbound adapter 第四批已经达到“范围可描述、实现闭环清晰、测试可回归、可以进入独立提交整理”的状态。下一步应继续维持边界，不把 plugin registry / setup migration / types surface 的相邻 diff 混入同一批提交。

## 当前阶段：channel account inspection + registry cache 第五批改造

### 目标

第五批当前收口的主题是 channel account inspection、config-backed directory fallback 与 channel registry cache，而不是更宽的 status 命令、setup 迁移或 action 行为改动。当前已确认纳入的子目标包括：

- 为 Slack / Discord / Telegram 提供统一的只读 account inspection helper
- 让 config-backed directory / group mention 逻辑改为使用 inspection 结果，而不是直接依赖各 channel runtime resolver
- 让 channel plugin registry lookup 在同一 registry 实例重新激活时刷新缓存
- 支持显式 `defaultAccount` 参与 account helper 的默认账号解析

### 当前已纳入范围

- account inspection helper
  - `src/slack/account-inspect.ts`
  - `src/discord/account-inspect.ts`
  - `src/telegram/account-inspect.ts`
  - `src/slack/accounts.ts`
  - `src/slack/accounts.test.ts`
- channel plugin helper / registry
  - `src/channels/plugins/account-helpers.ts`
  - `src/channels/plugins/account-helpers.test.ts`
  - `src/channels/plugins/index.ts`
- config-backed directory / group mention
  - `src/channels/plugins/directory-config.ts`
  - `src/channels/plugins/group-mentions.ts`
- 回归测试
  - `src/channels/plugins/plugins-core.test.ts`
  - `src/channels/plugins/group-mentions.test.ts`
  - `src/discord/accounts.test.ts`
  - `src/telegram/accounts.test.ts`

### 当前明确排除范围

- `src/channels/plugins/setup-helpers.ts`
- `src/commands/channels/add.ts`
- `src/channels/plugins/status.ts`
- `src/commands/channels/status.ts`
- `src/commands/status-all/**`
- `src/security/audit-channel.ts`
- `src/channels/plugins/message-action-names.ts`
- `src/channels/plugins/slack.actions.ts`

### 当前定向验证

当前这一批已通过的验证包括：

- `pnpm test -- src/slack/accounts.test.ts src/channels/plugins/account-helpers.test.ts src/channels/plugins/plugins-core.test.ts src/channels/plugins/group-mentions.test.ts src/discord/accounts.test.ts src/telegram/accounts.test.ts`

### 当前结论

截至当前，channel account inspection + registry cache 第五批已经达到“范围可描述、接口边界清晰、测试可回归、可以进入独立提交整理”的状态。下一步应继续维持边界，不把 status 命令、setup 迁移、action 行为等相邻主题混入同一批提交。

## 当前阶段：channel setup migration + onboarding secret input 第六批改造

### 目标

第六批当前收口的主题是 single-account 配置迁移、channel onboarding secret input 模式与 `channels add` 落盘行为，而不是更宽的 status / audit / action 主题。当前已确认纳入的子目标包括：

- 在为非 default 账号写入配置前，把 legacy single-account channel config 迁入 `accounts.default`
- 为 onboarding adapter 引入 secret ref / plaintext 两种 credential 输入模式
- 让 Slack / Discord / Telegram onboarding 在已配置 SecretRef 或 env 的情况下保持正确的“configured”判断
- 让 `channels add` 在 non-default 账号场景下复用同一套迁移逻辑

### 当前已纳入范围

- setup migration helper
  - `src/channels/plugins/setup-helpers.ts`
- onboarding shared helper / types
  - `src/channels/plugins/onboarding-types.ts`
  - `src/channels/plugins/onboarding/helpers.ts`
  - `src/channels/plugins/onboarding/helpers.test.ts`
- onboarding adapter
  - `src/channels/plugins/onboarding/slack.ts`
  - `src/channels/plugins/onboarding/discord.ts`
  - `src/channels/plugins/onboarding/telegram.ts`
- channels add CLI path
  - `src/commands/channels/add.ts`
  - `src/commands/channels.adds-non-default-telegram-account.test.ts`
  - `src/commands/channels.add.test.ts`
  - `src/channels/plugins/onboarding/telegram.test.ts`

### 当前明确排除范围

- `src/channels/plugins/status.ts`
- `src/commands/channels/status.ts`
- `src/commands/status-all/**`
- `src/security/audit-channel.ts`
- `src/channels/plugins/slack.actions.ts`
- `src/channels/plugins/message-action-names.ts`

### 当前定向验证

当前这一批已通过的验证包括：

- `pnpm test -- src/channels/plugins/onboarding/helpers.test.ts src/channels/plugins/onboarding/telegram.test.ts src/commands/channels.add.test.ts src/commands/channels.adds-non-default-telegram-account.test.ts`

### 当前结论

截至当前，channel setup migration + onboarding secret input 第六批已经达到“范围可描述、交互与落盘路径清晰、测试可回归、可以进入独立提交整理”的状态。下一步应继续维持边界，不把 status / audit / action 的相邻改动混入同一批提交。

## 当前阶段：channel status summary + readonly secret resolution 第七批改造

### 目标

第七批当前收口的主题是 channel 只读命令的 SecretRef 解析与状态摘要，而不是更宽的 security audit 或 runtime 行为改动。当前已确认纳入的子目标包括：

- 为只读命令提供 source config / resolved config 双视角摘要
- 在 config-only status / status-all summary 中保留“configured but unavailable”凭据状态
- 为 status 输出增加按 channel 聚合 issue 的共享 helper
- 让 capabilities / resolve / shared 命令路径走统一的只读 secret resolution

### 当前已纳入范围

- readonly account snapshot / inspect helper
  - `src/channels/account-snapshot-fields.ts`
  - `src/channels/read-only-account-inspect.ts`
  - `src/channels/plugins/status.ts`
- channel command shared/status paths
  - `src/commands/channels/shared.ts`
  - `src/commands/channels/resolve.ts`
  - `src/commands/channels/status.ts`
  - `src/commands/channels/capabilities.ts`
  - `src/commands/channels/capabilities.test.ts`
- status-all summary
  - `src/commands/status-all/channel-issues.ts`
  - `src/commands/status-all/channels.ts`
  - `src/commands/status-all/channels.mattermost-token-summary.test.ts`
  - `src/commands/status-all/diagnosis.ts`
  - `src/commands/status-all/report-lines.ts`
- summary helpers
  - `src/infra/channel-summary.ts`

### 当前明确排除范围

- `src/security/audit-channel.ts`
- `src/channels/plugins/message-action-names.ts`
- `src/channels/plugins/slack.actions.ts`
- Slack monitor / Discord monitor / Telegram runtime 相关行为变更

### 当前定向验证

当前这一批已通过的验证包括：

- `pnpm test -- src/channels/account-snapshot-fields.test.ts src/commands/channels.config-only-status-output.test.ts src/commands/status-all/channels.mattermost-token-summary.test.ts src/infra/channel-summary.test.ts src/commands/status-all/report-lines.test.ts src/commands/channels/capabilities.test.ts src/commands/status.scan.test.ts`

### 当前结论

截至当前，channel status summary + readonly secret resolution 第七批已经达到“范围可描述、CLI 路径收口清晰、测试可回归、可以进入独立提交整理”的状态。下一步应继续维持边界，不把 security audit 或 channel action 的相邻改动混入同一批提交。

## 当前阶段：channel action adapters 第八批改造

### 目标

第八批当前收口的主题是 channel message action adapter 的参数归一化与 reaction target 回退，而不是更宽的 action name 扩展、类型面扩张或 Slack monitor 行为重构。当前已确认纳入的子目标包括：

- 为 Discord / Signal / Telegram reaction action 复用同一套 `messageId -> toolContext.currentMessageId` 回退逻辑
- 让 Discord / Telegram poll / send 等适配器在进入底层 action tool 前先做一致的 boolean / number 归一化
- 让 Slack action adapter 透传受信任的 `mediaLocalRoots`，使发送动作与其余 channel action adapter 保持一致
- 为共享的 reaction messageId 回退 helper 补直接单测

### 当前已纳入范围

- channel action adapters
  - `src/channels/plugins/actions/discord/handle-action.ts`
  - `src/channels/plugins/actions/signal.ts`
  - `src/channels/plugins/actions/telegram.ts`
  - `src/channels/plugins/slack.actions.ts`
- shared action helper
  - `src/channels/plugins/actions/reaction-message-id.ts`
  - `src/channels/plugins/actions/reaction-message-id.test.ts`
- adapter regression tests
  - `src/channels/plugins/actions/actions.test.ts`

### 当前明确排除范围

- `src/channels/plugins/message-action-names.ts`
- `src/channels/plugins/types.core.ts`
- `src/channels/plugins/types.adapters.ts`
- `src/plugin-sdk/slack-message-actions.ts`
- `src/slack/message-actions.ts`
- `src/slack/monitor/**`

### 当前定向验证

当前这一批已通过的验证包括：

- `pnpm test -- src/channels/plugins/actions/actions.test.ts src/channels/plugins/actions/reaction-message-id.test.ts`

### 当前结论

截至当前，channel action adapters 第八批已经达到“边界收口清楚、行为都是适配层归一化、测试可直接回归、可以进入独立提交整理”的状态。下一步应继续维持边界，不把 action name 扩展、plugin-sdk Slack action、或 Slack monitor 的更大行为改动混入同一批提交。

## 当前阶段：message tool action surface 第九批改造

### 目标

第九批当前收口的主题是 message tool 对外暴露的 action schema / description 与 channel action surface 对齐，而不是更宽的 Slack monitor 或 plugin-sdk 内部行为改动。当前已确认纳入的子目标包括：

- 为通用 action name 清单补上 `poll-vote` 与 `download-file`
- 让 outbound action target spec 同步识别新增 action
- 让 Slack message actions 在 message action gate 打开时显式暴露 `download-file`
- 让 message tool 的 schema 在 scoped mode 下保留跨 channel action 校验能力，并按 Telegram poll gate 决定是否暴露 Telegram-only poll extra 参数
- 为上述 schema / description / Slack action surface 补直接回归

### 当前已纳入范围

- message tool schema / description
  - `src/agents/tools/message-tool.ts`
  - `src/agents/tools/message-tool.test.ts`
- shared action names / outbound spec
  - `src/channels/plugins/message-action-names.ts`
  - `src/infra/outbound/message-action-spec.ts`
- Slack action surface
  - `src/slack/message-actions.ts`
  - `src/slack/message-actions.test.ts`

### 当前明确排除范围

- `src/plugin-sdk/slack-message-actions.ts`
- `src/slack/monitor/**`
- `src/channels/plugins/actions/**`
- `src/channels/plugins/types.core.ts`
- `src/channels/plugins/types.adapters.ts`

### 当前定向验证

当前这一批已通过的验证包括：

- `pnpm test -- src/agents/tools/message-tool.test.ts src/slack/message-actions.test.ts src/plugin-sdk/slack-message-actions.test.ts`

### 当前结论

截至当前，message tool action surface 第九批已经达到“schema、description、action 枚举和 channel 暴露面一致、测试可直接回归、可以进入独立提交整理”的状态。下一步应继续维持边界，不把 plugin-sdk Slack adapter 或 Slack monitor 的更大改造混入同一批提交。

## 当前阶段：shared channel runtime guards 第十批改造

### 目标

第十批当前收口的主题是供多 channel 复用的入站 debounce、run busy 状态机和 typing start guard 共享 helper，而不是更宽的 Telegram / Discord / Slack monitor 行为迁移。当前已确认纳入的子目标包括：

- 为文本入站路径抽出统一的 debounce 判断与 per-channel debounce 工厂
- 为 channel worker 抽出统一的 `busy / activeRuns / lastRunActivityAt` 状态机
- 为 typing start 过程抽出可复用的 breaker guard，避免重复的 sealed / consecutive failure 处理
- 为这三类共享 helper 补直接单测

### 当前已纳入范围

- inbound debounce helper
  - `src/channels/inbound-debounce-policy.ts`
  - `src/channels/inbound-debounce-policy.test.ts`
- run state helper
  - `src/channels/run-state-machine.ts`
  - `src/channels/run-state-machine.test.ts`
- typing guard helper
  - `src/channels/typing-start-guard.ts`
  - `src/channels/typing-start-guard.test.ts`

### 当前明确排除范围

- `src/channels/session-meta.ts`
- `src/channels/thread-bindings-policy.ts`
- `src/channels/thread-bindings-messages.ts`
- `src/slack/monitor/**`
- `src/discord/monitor/**`
- `src/signal/monitor/**`
- `src/telegram/bot*.ts`

### 当前定向验证

当前这一批已通过的验证包括：

- `pnpm test -- src/channels/inbound-debounce-policy.test.ts src/channels/run-state-machine.test.ts src/channels/typing-start-guard.test.ts`

### 当前结论

截至当前，shared channel runtime guards 第十批已经达到“共享 helper 边界清楚、单测可直接回归、可以进入独立提交整理”的状态。下一步应继续维持边界，不把 session meta、thread binding 生命周期或具体 channel monitor 行为混入同一批提交。

## 当前阶段：slack delivery utilities 第十一批改造

### 目标

第十一批当前收口的主题是 Slack 非 monitor 的发送、文件下载、thread context、target 解析和 token 规范化工具层，而不是更宽的 Slack monitor 事件流重构。当前已确认纳入的子目标包括：

- 为 Slack file action 提供 `files.info -> resolveSlackMedia` 的下载路径，并在 channel/thread scope 明显不匹配时 fail-closed
- 将 Slack 上传路径切到 `getUploadURLExternal -> POST -> completeUploadExternal` 三段式流程，并把上传流量纳入 SSRF guard
- 在发送路径提前抑制纯 `NO_REPLY` 文本，避免无意义 API 调用
- 统一 Slack outbound text、modal metadata、DM target 解析、thread tool context 和 token 规范化的小型工具修正
- 为上述 utility 补直接回归，并补 agent tool / accounts 联动验证

### 当前已纳入范围

- Slack action / send utility
  - `src/slack/actions.ts`
  - `src/slack/actions.download-file.test.ts`
  - `src/slack/send.ts`
  - `src/slack/send.blocks.test.ts`
  - `src/slack/send.upload.test.ts`
- Slack formatting / metadata / targets / token
  - `src/slack/format.ts`
  - `src/slack/format.test.ts`
  - `src/slack/modal-metadata.ts`
  - `src/slack/modal-metadata.test.ts`
  - `src/slack/targets.ts`
  - `src/slack/token.ts`
- Slack threading utility
  - `src/slack/threading-tool-context.ts`
  - `src/slack/threading-tool-context.test.ts`
  - `src/slack/threading.test.ts`

### 当前明确排除范围

- `src/slack/sent-thread-cache.ts`
- `src/slack/sent-thread-cache.test.ts`
- `src/slack/monitor/**`
- `src/slack/message-actions.ts`
- `src/plugin-sdk/slack-message-actions.ts`

### 当前定向验证

当前这一批已通过的验证包括：

- `pnpm test -- src/slack/actions.download-file.test.ts src/slack/format.test.ts src/slack/modal-metadata.test.ts src/slack/send.blocks.test.ts src/slack/send.upload.test.ts src/slack/threading-tool-context.test.ts src/slack/threading.test.ts`
- `pnpm test -- src/agents/tools/slack-actions.test.ts src/slack/accounts.test.ts`

### 当前结论

截至当前，slack delivery utilities 第十一批已经达到“工具层边界清楚、现有调用面可联动验证、可以进入独立提交整理”的状态。下一步应继续维持边界，不把 sent-thread-cache 或 Slack monitor 事件流重构混入同一批提交。

## 当前阶段：slack sent-thread participation cache 第十二批改造

### 目标

第十二批当前收口的主题是 Slack 已发线程参与缓存，用来支撑 thread auto-reply 的“机器人已参与该线程”判定，而不是更宽的 Slack monitor reply 策略重构。当前已确认纳入的子目标包括：

- 为 Slack 线程参与记录提供 account/channel/thread 三级作用域缓存
- 提供 TTL、容量上限与按读清理过期项的轻量内存策略
- 保持 `agents/tools/slack-actions.ts` 现有调用面可直接联动
- 为缓存本身和 tool 入口做回归

### 当前已纳入范围

- Slack sent-thread cache
  - `src/slack/sent-thread-cache.ts`
  - `src/slack/sent-thread-cache.test.ts`

### 当前明确排除范围

- `src/slack/monitor/replies.ts`
- `src/slack/monitor/message-handler/prepare.ts`
- `src/slack/monitor/message-handler/dispatch.ts`
- `src/slack/monitor/**` 其他事件流重构

### 当前定向验证

当前这一批已通过的验证包括：

- `pnpm test -- src/slack/sent-thread-cache.test.ts src/agents/tools/slack-actions.test.ts`

### 当前结论

截至当前，slack sent-thread participation cache 第十二批已经达到“缓存边界清楚、现有调用面可联动验证、可以进入独立提交整理”的状态。下一步应继续维持边界，不把 Slack monitor reply 策略与 thread auto-reply 行为混入同一批提交。

## 当前阶段：slack monitor auth/context normalization 第十三批改造

### 目标

第十三批当前收口的主题是 Slack monitor 的授权、allow-from 缓存、channel type 归一化和 system event session key 上下文，而不是更宽的 message-handler thread auto-reply / debounce 行为。当前已确认纳入的子目标包括：

- 为 Slack allow-from 解析加上 base/pairing 分层缓存与 TTL
- 抽出 DM 授权和 system event sender 授权的共享逻辑
- 抽出独立的 Slack channel type 推断 / 归一化 helper
- 让 monitor context 保留 channel config key 集合、typing reaction 和 sender-aware system event session routing
- 为上述授权与上下文层补直接回归，并验证既有 monitor suite 不回退

### 当前已纳入范围

- allow-list / auth helpers
  - `src/slack/monitor/allow-list.ts`
  - `src/slack/monitor/auth.ts`
  - `src/slack/monitor/auth.test.ts`
  - `src/slack/monitor/dm-auth.ts`
- monitor context / channel type
  - `src/slack/monitor/channel-type.ts`
  - `src/slack/monitor/context.ts`
  - `src/slack/monitor/context.test.ts`

### 当前明确排除范围

- `src/slack/monitor/message-handler.ts`
- `src/slack/monitor/message-handler/**`
- `src/slack/monitor/replies.ts`
- `src/slack/monitor/events/**`
- `src/slack/monitor/slash.ts`

### 当前定向验证

当前这一批已通过的验证包括：

- `pnpm test -- src/slack/monitor/auth.test.ts src/slack/monitor/context.test.ts`
- `pnpm test -- src/slack/monitor/monitor.test.ts`

### 当前结论

截至当前，slack monitor auth/context normalization 第十三批已经达到“授权与上下文层边界清楚、回归可直接验证、可以进入独立提交整理”的状态。下一步应继续维持边界，不把 message-handler thread/debounce 与 events/slash 行为混入同一批提交。

## 当前阶段：slack monitor message-handler thread/debounce 第十四批改造

### 目标

第十四批当前收口的主题是 Slack monitor `message-handler/prepare/dispatch/replies` 这条消息处理子线的 thread session、debounce、app_mention race、reply identity 与内容组装，而不是更宽的 events/slash 分发层。当前已确认纳入的子目标包括：

- 为 Slack message handler 引入 thread-aware debounce key，避免不同顶层消息串到同一回复
- 处理 message / app_mention 并发到达时的 race，允许一次补偿重试但仍保持最终去重
- 将 thread starter / thread history / inherited parent file 过滤等逻辑拆到共享 helper
- 让 reply delivery 支持 identity 透传，并把 body/command body/thread starter 写入上下文时的边界收紧
- 为上述行为补直接回归

### 当前已纳入范围

- message handler core
  - `src/slack/monitor/message-handler.ts`
  - `src/slack/monitor/message-handler.test.ts`
  - `src/slack/monitor/message-handler.app-mention-race.test.ts`
  - `src/slack/monitor/message-handler.debounce-key.test.ts`
- prepare / dispatch helpers
  - `src/slack/monitor/message-handler/prepare.ts`
  - `src/slack/monitor/message-handler/prepare.test.ts`
  - `src/slack/monitor/message-handler/prepare.thread-session-key.test.ts`
  - `src/slack/monitor/message-handler/prepare-content.ts`
  - `src/slack/monitor/message-handler/prepare-thread-context.ts`
  - `src/slack/monitor/message-handler/dispatch.ts`
  - `src/slack/monitor/message-handler/types.ts`
- reply delivery
  - `src/slack/monitor/replies.ts`
  - `src/slack/monitor/replies.test.ts`

### 当前明确排除范围

- `src/slack/monitor/events/**`
- `src/slack/monitor/slash.ts`
- `src/slack/monitor/provider.ts`
- `src/slack/monitor/monitor.test.ts`

### 当前定向验证

当前这一批已通过的验证包括：

- `pnpm test -- src/slack/monitor/message-handler.test.ts src/slack/monitor/message-handler.app-mention-race.test.ts src/slack/monitor/message-handler.debounce-key.test.ts src/slack/monitor/message-handler/prepare.test.ts src/slack/monitor/message-handler/prepare.thread-session-key.test.ts src/slack/monitor/replies.test.ts`

### 当前结论

截至当前，slack monitor message-handler thread/debounce 第十四批已经达到“处理链路边界清楚、回归可直接验证、可以进入独立提交整理”的状态。下一步应继续维持边界，不把 events/slash/provider 的更大分发层混入同一批提交。

## 当前阶段：slack monitor system events 第十五批改造

### 目标

第十五批当前收口的主题是 Slack monitor `events/*` 的 system event 授权、subtype 元数据与 trackEvent 透传，而不是更宽的 slash/provider/media 层。当前已确认纳入的子目标包括：

- 为 interaction / member / pin / reaction / message subtype system events 复用统一的 sender 授权与 session context 解析
- 为 message subtype 解析抽出共享 handler metadata
- 让 channel config 查找支持 Slack channel id 大小写兼容
- 让 event registry 把 `trackEvent` 透传到各类 system event handler
- 为上述 system event 分发层补直接回归

### 当前已纳入范围

- event registry / shared context
  - `src/slack/monitor/events.ts`
  - `src/slack/monitor/events/system-event-context.ts`
  - `src/slack/monitor/events/system-event-test-harness.ts`
  - `src/slack/monitor/channel-config.ts`
  - `src/slack/monitor/types.ts`
- event handlers
  - `src/slack/monitor/events/channels.ts`
  - `src/slack/monitor/events/interactions.ts`
  - `src/slack/monitor/events/interactions.modal.ts`
  - `src/slack/monitor/events/members.ts`
  - `src/slack/monitor/events/messages.ts`
  - `src/slack/monitor/events/pins.ts`
  - `src/slack/monitor/events/reactions.ts`
  - `src/slack/monitor/events/message-subtype-handlers.ts`
- event tests
  - `src/slack/monitor/events/channels.test.ts`
  - `src/slack/monitor/events/interactions.test.ts`
  - `src/slack/monitor/events/members.test.ts`
  - `src/slack/monitor/events/messages.test.ts`
  - `src/slack/monitor/events/pins.test.ts`
  - `src/slack/monitor/events/reactions.test.ts`
  - `src/slack/monitor/events/message-subtype-handlers.test.ts`

### 当前明确排除范围

- `src/slack/monitor/slash.ts`
- `src/slack/monitor/slash.test.ts`
- `src/slack/monitor/provider.ts`
- `src/slack/monitor/media.ts`
- `src/slack/monitor/monitor.test.ts`

### 当前定向验证

当前这一批已通过的验证包括：

- `pnpm test -- src/slack/monitor/events/interactions.test.ts src/slack/monitor/events/messages.test.ts src/slack/monitor/events/pins.test.ts src/slack/monitor/events/reactions.test.ts src/slack/monitor/events/channels.test.ts src/slack/monitor/events/members.test.ts src/slack/monitor/events/message-subtype-handlers.test.ts`

### 当前结论

截至当前，slack monitor system events 第十五批已经达到“事件分发层边界清楚、system event 授权与 session context 已收口、可以进入独立提交整理”的状态。下一步应继续维持边界，不把 slash/provider/media 的更大运行时层混入同一批提交。

## 当前阶段：slack monitor media hardening 第十六批改造

### 目标

第十六批当前收口的主题是 Slack monitor 媒体下载的 SSRF 策略与 HTML/auth-page 防护，而不是更宽的 provider / slash 运行时层。当前已确认纳入的子目标包括：

- 为 Slack 文件下载和转发附件图片下载统一附加 Slack CDN SSRF allowlist
- 拒绝把意外返回的 HTML 登录页当成二进制媒体落盘
- 允许预期的 `.html` / `text/html` 文件继续通过
- 为上述媒体防护补直接回归

### 当前已纳入范围

- media resolver
  - `src/slack/monitor/media.ts`
  - `src/slack/monitor/media.test.ts`

### 当前明确排除范围

- `src/slack/monitor/provider.ts`
- `src/slack/monitor/slash.ts`
- `src/slack/monitor/monitor.test.ts`

### 当前定向验证

当前这一批已通过的验证包括：

- `pnpm test -- src/slack/monitor/media.test.ts`

### 当前结论

截至当前，slack monitor media hardening 第十六批已经达到“媒体下载策略边界清楚、回归可直接验证、可以进入独立提交整理”的状态。下一步应继续维持边界，不把 provider/slash 运行时层混入同一批提交。

## 当前阶段：slack monitor provider reconnect 第十七批改造

### 目标

第十七批当前收口的主题是 Slack provider 的启动/断线重连策略与 auth fail-fast，而不是更宽的 slash command 或 monitor 总体验证。当前已确认纳入的子目标包括：

- 为 Slack socket mode 引入受控重连和 backoff
- 对非可恢复 auth 错误 fail-fast，避免网关无限重试卡死
- 在 account disabled 时跳过启动并等待 abort
- 通过 `trackEvent` 更新 `lastEventAt/lastInboundAt`，帮助识别半死连接
- 为 auth error 分类与 reconnect helper 补直接回归

### 当前已纳入范围

- provider / reconnect helpers
  - `src/slack/monitor/provider.ts`
  - `src/slack/monitor/reconnect-policy.ts`
- provider tests
  - `src/slack/monitor/provider.auth-errors.test.ts`
  - `src/slack/monitor/provider.reconnect.test.ts`

### 当前明确排除范围

- `src/slack/monitor/slash.ts`
- `src/slack/monitor/slash.test.ts`
- `src/slack/monitor/monitor.test.ts`

### 当前定向验证

当前这一批已通过的验证包括：

- `pnpm test -- src/slack/monitor/provider.auth-errors.test.ts src/slack/monitor/provider.reconnect.test.ts`

### 当前结论

截至当前，slack monitor provider reconnect 第十七批已经达到“启动与重连策略边界清楚、回归可直接验证、可以进入独立提交整理”的状态。下一步应继续维持边界，不把 slash command 运行时或 monitor 回归补丁混入同一批提交。

## 当前阶段：slack monitor slash runtime 第十八批改造

### 目标

第十八批当前收口的主题是 Slack slash command 注册、dispatch runtime 懒加载、交互菜单回退和 mismatch 保护，而不是更宽的 provider 或 monitor 总体验证。当前已确认纳入的子目标包括：

- 将 slash command / dispatch / skill command 逻辑拆到 runtime 边界，避免混合动态导入
- 让 slash payload、arg action、arg options 都经过 app/team mismatch 保护
- 在 `app.options()` 注册失败时回退到 static menu，而不是拖垮整个 provider 启动
- 对 native command alias（如 `/agentstatus -> /status`）与 session meta 记录路径保持一致
- 为上述 slash 运行时行为补回归

### 当前已纳入范围

- slash runtime
  - `src/slack/monitor/slash.ts`
  - `src/slack/monitor/slash-commands.runtime.ts`
  - `src/slack/monitor/slash-dispatch.runtime.ts`
  - `src/slack/monitor/slash-skill-commands.runtime.ts`
- slash tests
  - `src/slack/monitor/slash.test.ts`

### 当前明确排除范围

- `src/slack/monitor/monitor.test.ts`
- `src/slack/monitor/provider.ts`
- `src/slack/monitor/media.ts`

### 当前定向验证

当前这一批已通过的验证包括：

- `pnpm test -- src/slack/monitor/slash.test.ts`

### 当前结论

截至当前，slack monitor slash runtime 第十八批已经达到“slash 运行时边界清楚、交互菜单与授权回退可验证、可以进入独立提交整理”的状态。下一步应继续维持边界，不把 monitor 总体验证或其他 channel 主题混入同一批提交。

## 当前阶段：slack regression coverage cleanup 第十九批改造

### 目标

第十九批当前收口的主题是 Slack 余下的回归测试补丁与小型辅助修正，用来给前面多批 Slack 改动补覆盖，而不是新增运行时行为。当前已确认纳入的子目标包括：

- 为 Slack monitor 总体验证补上 disabled account、system event routing、thread session 与 responsePrefix 等回归
- 为 stream mode 兼容映射补覆盖
- 让 prepare 测试辅助 context 与当前 `typingReaction` 字段对齐
- 让 directory live 读取优先复用 resolved `userToken`

### 当前已纳入范围

- Slack regression tests / helpers
  - `src/slack/monitor.tool-result.test.ts`
  - `src/slack/monitor/monitor.test.ts`
  - `src/slack/monitor/message-handler/prepare.test-helpers.ts`
  - `src/slack/stream-mode.test.ts`
  - `src/slack/directory-live.ts`

### 当前明确排除范围

- 非 Slack 主题的任何 channel/runtime 改动

### 当前定向验证

当前这一批已通过的验证包括：

- `pnpm test -- src/slack/monitor.tool-result.test.ts src/slack/stream-mode.test.ts src/slack/monitor/monitor.test.ts`

### 当前结论

截至当前，slack regression coverage cleanup 第十九批已经达到“Slack 剩余零散项已收口、回归覆盖补齐、可以进入独立提交整理”的状态。下一步应转向下一条非 Slack 主线。

## 当前阶段：shared typing callbacks 第二十批改造

### 目标

第二十批当前收口的主题是共享 typing callback 的 keepalive breaker 与 TTL 安全控制，而不是更宽的 session / allowlist / dock 配置层。当前已确认纳入的子目标包括：

- 让 typing keepalive 复用前面抽出的 `typing-start-guard`
- 为连续 start 失败增加 breaker，避免无限重试
- 为 typing 指示器增加最大持续时长 TTL，防止异常路径下长期悬挂
- 为上述行为补直接回归

### 当前已纳入范围

- typing callbacks
  - `src/channels/typing.ts`
  - `src/channels/typing.test.ts`

### 当前明确排除范围

- `src/channels/session.ts`
- `src/channels/allow-from.ts`
- `src/channels/dock.ts`

### 当前定向验证

当前这一批已通过的验证包括：

- `pnpm test -- src/channels/typing.test.ts`

### 当前结论

截至当前，shared typing callbacks 第二十批已经达到“行为边界清楚、回归可直接验证、可以进入独立提交整理”的状态。下一步应继续拆 allowlist、dock/config inspector 和 session 路径。

## 当前阶段：shared allowlist and target helpers 第二十一批改造

### 目标

第二十一批当前收口的主题是 DM/group allow-from 合并、allowlist candidate 匹配与 `@user` target 解析 helper，而不是更宽的 dock/config inspector 或 session 路径。当前已确认纳入的子目标包括：

- 将 DM allow-from 合并与 group allow-from fallback 规则显式拆分
- 为 allowlist 候选匹配增加共享实现与轻量缓存
- 为 allowlist resolution summary 支持自定义 unresolved 格式
- 为 channel target 解析补充统一的 mention/prefix/@user 组合 helper

### 当前已纳入范围

- allow-from / allowlist helpers
  - `src/channels/allow-from.ts`
  - `src/channels/allow-from.test.ts`
  - `src/channels/allowlist-match.ts`
  - `src/channels/allowlists/resolve-utils.ts`
  - `src/channels/allowlists/resolve-utils.test.ts`
- target parsing helper
  - `src/channels/targets.ts`

### 当前明确排除范围

- `src/channels/dock.ts`
- `src/channels/session.ts`
- `src/channels/account-summary.ts`

### 当前定向验证

当前这一批已通过的验证包括：

- `pnpm test -- src/channels/allow-from.test.ts src/channels/allowlists/resolve-utils.test.ts`

### 当前结论

截至当前，shared allowlist and target helpers 第二十一批已经达到“共享 helper 边界清楚、直接回归可验证、可以进入独立提交整理”的状态。下一步应继续拆 dock/config inspector 和 session 路径。

## 当前阶段：dock config inspector and heartbeat 第二十二批改造

### 目标

第二十二批当前收口的主题是 channel dock 配置读取、只读 account snapshot 投影与 WhatsApp heartbeat recipient 解析，而不是更宽的 session / thread-binding / ACP 路径。当前已确认纳入的子目标包括：

- 让 Telegram / Slack / Discord 的 dock 配置读取复用 inspection helper，保留 SecretRef 未解引用时的只读状态
- 让 WhatsApp / iMessage 的 dock allow-from 与 defaultTo 读取复用共享 config helper
- 让 channel summary/status 命令统一复用轻量 `enabled/configured` 解析与安全 account 字段投影
- 让 WhatsApp heartbeat 读取 pairing allow-from store 时显式绑定默认 account 语义
- 为上述行为补齐 dock / heartbeat / channel summary 的直接回归

### 当前已纳入范围

- dock/config readers
  - `src/channels/dock.ts`
  - `src/channels/dock.test.ts`
- account summary helpers
  - `src/channels/account-summary.ts`
  - `src/infra/channel-summary.test.ts`
- WhatsApp heartbeat
  - `src/channels/plugins/whatsapp-heartbeat.ts`
  - `src/channels/plugins/whatsapp-heartbeat.test.ts`

### 当前明确排除范围

- `src/channels/session.ts`
- `src/channels/session.test.ts`
- `src/discord/monitor/agent-components.ts`
- `src/imessage/monitor/monitor-provider.ts`
- `src/signal/monitor/event-handler.ts`
- `src/line/bot-message-context.ts`

### 当前定向验证

当前这一批已通过的验证包括：

- `pnpm test -- src/channels/dock.test.ts src/channels/plugins/whatsapp-heartbeat.test.ts src/infra/channel-summary.test.ts src/commands/status-all/channels.mattermost-token-summary.test.ts`

### 当前结论

截至当前，dock config inspector and heartbeat 第二十二批已经达到“只读配置语义收口、状态摘要回归可验证、可以进入独立提交整理”的状态。下一步应单独处理 session last-route owner pin 那条跨多个 channel 调用点的主线。

## 当前阶段：discord provider lifecycle and listener timeouts 第二十三批改造

### 目标

第二十三批当前收口的主题是 Discord monitor provider 生命周期、listener 超时保护与早期 gateway error 护栏，而不是更宽的 thread bindings、message handler 或 ACP 路径。当前已确认纳入的子目标包括：

- 为 Discord gateway 生命周期增加早期 error guard，避免 listener 注册前的 fatal gateway error 丢失
- 为 gateway reconnect / HELLO stall 增加共享 armable watchdog 与强制停机路径
- 为 Discord listener 运行增加可中止超时封装，确保同 channel 队列不会被悬挂 handler 卡死
- 为 provider status patch 与 timeout helper 拆出共享 contract，收口 provider / lifecycle / listeners 之间的状态交互
- 为 allowlist 解析、provider lifecycle、listener timeout、watchdog 行为补齐直接回归

### 当前已纳入范围

- shared transport helper
  - `src/channels/transport/stall-watchdog.ts`
  - `src/channels/transport/stall-watchdog.test.ts`
- discord provider lifecycle
  - `src/discord/monitor/provider.ts`
  - `src/discord/monitor/provider.test.ts`
  - `src/discord/monitor/provider.lifecycle.ts`
  - `src/discord/monitor/provider.lifecycle.test.ts`
  - `src/discord/monitor/gateway-error-guard.ts`
  - `src/discord/monitor/gateway-error-guard.test.ts`
  - `src/discord/monitor/status.ts`
  - `src/discord/monitor/timeouts.ts`
- discord listeners / allowlist runtime
  - `src/discord/monitor/listeners.ts`
  - `src/discord/monitor/listeners.test.ts`
  - `src/discord/monitor/provider.allowlist.ts`
  - `src/discord/monitor/provider.allowlist.test.ts`

### 当前明确排除范围

- `src/discord/monitor/message-handler.process.ts`
- `src/discord/monitor/agent-components.ts`
- `src/discord/monitor/thread-bindings.*`
- `src/channels/session.ts`
- `src/telegram/**`

### 当前定向验证

当前这一批已通过的验证包括：

- `pnpm test -- src/channels/transport/stall-watchdog.test.ts src/discord/monitor/gateway-error-guard.test.ts src/discord/monitor/listeners.test.ts src/discord/monitor/provider.allowlist.test.ts src/discord/monitor/provider.lifecycle.test.ts src/discord/monitor/provider.test.ts`

### 当前结论

截至当前，discord provider lifecycle and listener timeouts 第二十三批已经达到“生命周期护栏、listener 超时恢复与状态 contract 收口清楚、可以进入独立提交整理”的状态。下一步应继续避开 message-handler / thread-bindings / ACP 主线，优先处理仍能独立成批的共享 session 或 Telegram 较小支线。

## 当前阶段：channel contract inspection and binding resolution 第二十四批改造

### 目标

第二十四批当前收口的主题是 channel plugin contract 扩展、binding accountId 决策、gateway channel runtime 透传与 security audit 的只读 inspection fallback，而不是更宽的 session/thread-binding/Telegram 路径。当前已确认纳入的子目标包括：

- 为 channel plugin config adapter 增加 `inspectAccount`、`resolveBindingAccountId` 和 gateway `channelRuntime` contract
- 为 agent bindings 统一收口 accountId 选择，优先复用 channel setup adapter，再回退到 `forceAccountBinding` 默认账号语义
- 为 gateway channel manager 透传 plugin runtime 的 channel helper，支持外部 channel plugin 获取高级 runtime 能力
- 为 security audit 统一复用 `inspectAccount` + read-only fallback，保留 source config 中“configured but unavailable”凭据状态
- 为 config-only status / security audit / gateway channel manager / agent binding 行为补齐直接回归

### 当前已纳入范围

- shared channel contracts
  - `src/channels/plugins/types.core.ts`
  - `src/channels/plugins/types.adapters.ts`
- binding / gateway runtime plumbing
  - `src/commands/agents.bindings.ts`
  - `src/commands/agents.test.ts`
  - `src/gateway/server-channels.ts`
  - `src/gateway/server-channels.test.ts`
- read-only inspection / audit coverage
  - `src/security/audit-channel.ts`
  - `src/security/audit.test.ts`
  - `src/commands/channels.config-only-status-output.test.ts`

### 当前明确排除范围

- `src/channels/session.ts`
- `src/channels/thread-bindings-*`
- `src/discord/monitor/thread-bindings.*`
- `src/auto-reply/reply/commands-acp*`
- `src/telegram/**`

### 当前定向验证

当前这一批已通过的验证包括：

- `pnpm test -- src/commands/agents.test.ts src/commands/channels.config-only-status-output.test.ts src/gateway/server-channels.test.ts src/security/audit.test.ts`

### 当前结论

截至当前，channel contract inspection and binding resolution 第二十四批已经达到“shared contract、binding 决策与 audit fallback 收口清楚、可以进入独立提交整理”的状态。下一步应继续避开 thread-binding / ACP / Telegram 大主线，优先处理仍能独立拆开的较小批次。

## 当前阶段：line imessage signal inbound gating 第二十五批改造

### 目标

第二十五批当前收口的主题是 Line / iMessage / Signal 的入站 gating、command authorization 透传、session last-route owner pin 与 monitor 生命周期补强，而不是更宽的 Discord thread-binding、ACP 或 Telegram 路径。当前已确认纳入的子目标包括：

- 为 Line message/postback 入站统一返回 `allowed + commandAuthorized`，并把授权结果透传进 context
- 让 Line group/room peer 绑定改为匹配原始 id，同时补上 group system prompt 与 webhook redelivery 去重回归
- 为 iMessage / Signal 的 DM control command 授权统一复用 command gate，而不是私聊走额外放宽路径
- 为 iMessage / Signal / Line 的主会话 last-route 更新增加 pinned DM owner 保护，避免非 owner 私聊覆盖 main session 路由
- 为 Line monitor webhook 注册与 abort 生命周期补幂等/等待逻辑，为 Signal 入站 debounce/gating 收口共享 helper

### 当前已纳入范围

- shared session guard
  - `src/channels/session.ts`
  - `src/channels/session.test.ts`
- line inbound handlers / monitor
  - `src/line/bot-message-context.ts`
  - `src/line/bot-message-context.test.ts`
  - `src/line/bot-handlers.ts`
  - `src/line/bot-handlers.test.ts`
  - `src/line/bot.ts`
  - `src/line/monitor.ts`
  - `src/line/monitor.lifecycle.test.ts`
- imessage inbound gating
  - `src/imessage/monitor/inbound-processing.ts`
  - `src/imessage/monitor/inbound-processing.test.ts`
  - `src/imessage/monitor/monitor-provider.ts`
  - `src/imessage/monitor.gating.test.ts`
- signal inbound contract / debounce
  - `src/signal/monitor.ts`
  - `src/signal/monitor/event-handler.ts`
  - `src/signal/monitor/event-handler.types.ts`
  - `src/signal/monitor/event-handler.inbound-contract.test.ts`
  - `src/signal/monitor/event-handler.mention-gating.test.ts`

### 当前明确排除范围

- `src/discord/monitor/agent-components.ts`
- `src/discord/monitor/message-handler.process.ts`
- `src/discord/monitor/thread-bindings.*`
- `src/telegram/**`
- `src/auto-reply/reply/commands-acp*`

### 当前定向验证

当前这一批已通过的验证包括：

- `pnpm test -- src/channels/session.test.ts src/line/bot-message-context.test.ts src/line/bot-handlers.test.ts src/line/monitor.lifecycle.test.ts src/imessage/monitor/inbound-processing.test.ts src/imessage/monitor.gating.test.ts src/signal/monitor/event-handler.inbound-contract.test.ts src/signal/monitor/event-handler.mention-gating.test.ts`

### 当前结论

截至当前，line imessage signal inbound gating 第二十五批已经达到“入站 gating、command auth、main-session owner pin 与 monitor 生命周期收口清楚、可以进入独立提交整理”的状态。下一步应继续避开 ACP / Telegram 大主线，优先处理仍可独立拆开的 Discord 或 thread-binding 较小支线。

## 当前阶段：discord thread bindings lifecycle 第二十六批改造

### 目标

第二十六批当前收口的主题是 Discord thread bindings 的共享 policy/helper、生命周期管理、persona/intro/farewell 文案与 thread-session close 行为，而不是更宽的 Discord message handler、ACP delivery 或 Telegram 路径。当前已确认纳入的子目标包括：

- 抽出共享 session meta safe writer 与 thread-binding policy/helper，统一 idle/max-age/spawn gate 计算
- 为 Discord thread binding manager 增加 lifecycle sweep、persona、starter fallback 和 stale session close 的共享实现
- 让 thread binding 在 thread 被删除、idle 过期、max-age 过期时都能稳定解绑并写出系统提示
- 为 thread starter 解析、persona 构造、shared-state 共享与 session stale close 补直接回归

### 当前已纳入范围

- shared channel helpers
  - `src/channels/session-meta.ts`
  - `src/channels/thread-bindings-policy.ts`
  - `src/channels/thread-bindings-messages.ts`
- discord thread bindings runtime
  - `src/discord/monitor/thread-bindings.ts`
  - `src/discord/monitor/thread-bindings.types.ts`
  - `src/discord/monitor/thread-bindings.state.ts`
  - `src/discord/monitor/thread-bindings.manager.ts`
  - `src/discord/monitor/thread-bindings.lifecycle.ts`
  - `src/discord/monitor/thread-bindings.messages.ts`
  - `src/discord/monitor/thread-bindings.discord-api.ts`
  - `src/discord/monitor/thread-bindings.config.ts`
  - `src/discord/monitor/thread-bindings.persona.ts`
  - `src/discord/monitor/thread-session-close.ts`
  - `src/discord/monitor/threading.ts`
- direct regression coverage
  - `src/discord/monitor/thread-bindings.lifecycle.test.ts`
  - `src/discord/monitor/thread-bindings.persona.test.ts`
  - `src/discord/monitor/thread-bindings.shared-state.test.ts`
  - `src/discord/monitor/thread-bindings.discord-api.test.ts`
  - `src/discord/monitor/thread-session-close.test.ts`
  - `src/discord/monitor/threading.starter.test.ts`

### 当前明确排除范围

- `src/discord/monitor/message-handler.process.ts`
- `src/discord/monitor/agent-components.ts`
- `src/auto-reply/reply/commands-acp*`
- `src/telegram/**`

### 当前定向验证

当前这一批已通过的验证包括：

- `pnpm test -- src/discord/monitor/thread-bindings.lifecycle.test.ts src/discord/monitor/thread-bindings.persona.test.ts src/discord/monitor/thread-session-close.test.ts src/discord/monitor/thread-bindings.shared-state.test.ts src/discord/monitor/thread-bindings.discord-api.test.ts src/discord/monitor/threading.starter.test.ts`

### 当前结论

截至当前，discord thread bindings lifecycle 第二十六批已经达到“thread-binding policy、manager lifecycle、persona/starter fallback 与 stale session close 收口清楚、可以进入独立提交整理”的状态。下一步应继续避开 Telegram 大主线，优先处理剩余仍能拆开的 Discord smaller surface 或 ACP 支线。

## 当前阶段：discord token probe and directory resolution 第二十七批改造

### 目标

第二十七批当前收口的主题是 Discord token 规范化、probe fallback、channel/user allowlist 解析、directory 缓存与 audit/ui 的 inspection 路径，而不是更宽的 Discord message-handler/voice/Telegram 路径。当前已确认纳入的子目标包括：

- 让 Discord token 解析显式感知配置路径，区分“账号显式配置为空”与“可继续回退到顶层/env”的情况
- 为 Discord probe 增加从 bot token 直接解析 application id 的 fallback，减少网络失败时的启动阻塞
- 让 channel allowlist 解析正确处理 guild/channel numeric id、跨 guild mismatch、403/404 与 slug fallback
- 为 live directory lookup 增加用户缓存写入，为 audit/ui 改为复用 inspection helper 而不是直接 resolve runtime account
- 为上述 token/probe/resolve/directory/audit 行为补齐直接回归

### 当前已纳入范围

- token / probe / account inspection
  - `src/discord/token.ts`
  - `src/discord/token.test.ts`
  - `src/discord/probe.ts`
  - `src/discord/probe.parse-token.test.ts`
  - `src/discord/client.ts`
  - `src/discord/ui.ts`
  - `src/discord/accounts.test.ts`
- allowlist resolution / directory cache
  - `src/discord/resolve-channels.ts`
  - `src/discord/resolve-channels.test.ts`
  - `src/discord/resolve-users.ts`
  - `src/discord/directory-live.ts`
  - `src/discord/directory-cache.ts`
- audit coverage
  - `src/discord/audit.ts`
  - `src/discord/audit.test.ts`

### 当前明确排除范围

- `src/discord/monitor/message-handler.*`
- `src/discord/monitor/agent-components.ts`
- `src/discord/voice/**`
- `src/telegram/**`

### 当前定向验证

当前这一批已通过的验证包括：

- `pnpm test -- src/discord/accounts.test.ts src/discord/audit.test.ts src/discord/token.test.ts src/discord/probe.parse-token.test.ts src/discord/probe.intents.test.ts src/discord/resolve-channels.test.ts src/discord/resolve-users.test.ts src/discord/directory-live.test.ts`

### 当前结论

截至当前，discord token probe and directory resolution 第二十七批已经达到“token/probe fallback、allowlist 解析与 directory/audit inspection 收口清楚、可以进入独立提交整理”的状态。下一步应继续避开 Telegram 大主线，优先处理 Discord monitor 剩余可拆小面或 ACP 支线。

## 当前阶段：discord send and reply delivery 第二十八批改造

### 目标

第二十八批当前收口的主题是 Discord outbound send/reply delivery、target 解析与 component wildcard custom-id 处理，而不是更宽的 Discord monitor/voice/Telegram 路径。当前已确认纳入的子目标包括：

- 让 reply delivery 复用更轻量的 thread-binding lookup contract，并在分块发送后回写 thread activity
- 让 Discord outbound send 在 forum/media/thread/webhook 路径下减少重复 target/channel 解析与重试器创建
- 为 target 解析增加默认 `channel:` 语义，并把目录命中的 user 写回缓存
- 让 component custom-id parser 显式忽略 wildcard registration id，避免把注册占位符误当成真实交互
- 为 thread create、basic channel send、webhook activity、reply delivery 与 target parsing 补齐直接回归

### 当前已纳入范围

- reply delivery / thread binding lookup
  - `src/discord/monitor/reply-delivery.ts`
  - `src/discord/monitor/reply-delivery.test.ts`
- outbound send / targets
  - `src/discord/send.components.ts`
  - `src/discord/send.messages.ts`
  - `src/discord/send.outbound.ts`
  - `src/discord/send.reactions.ts`
  - `src/discord/send.shared.ts`
  - `src/discord/send.types.ts`
  - `src/discord/send.creates-thread.test.ts`
  - `src/discord/send.sends-basic-channel-messages.test.ts`
  - `src/discord/send.webhook-activity.test.ts`
  - `src/discord/targets.ts`
  - `src/discord/components.ts`

### 当前明确排除范围

- `src/discord/monitor/message-handler.*`
- `src/discord/monitor/agent-components.ts`
- `src/discord/voice/**`
- `src/telegram/**`

### 当前定向验证

当前这一批已通过的验证包括：

- `pnpm test -- src/discord/monitor/reply-delivery.test.ts src/discord/send.creates-thread.test.ts src/discord/send.sends-basic-channel-messages.test.ts src/discord/send.webhook-activity.test.ts src/discord/targets.test.ts`

### 当前结论

截至当前，discord send and reply delivery 第二十八批已经达到“outbound send、reply delivery 与 target/component 解析收口清楚、可以进入独立提交整理”的状态。下一步应继续避开 Telegram 大主线，优先处理 Discord monitor 剩余较小支线或 ACP 支线。

## 当前阶段：discord voice pipeline 第二十九批改造

### 目标

第二十九批当前收口的主题是 Discord voice upload/codec pipeline、voice command owner gating 与 voice manager 的 speaker context/owner detection，而不是更宽的 Discord monitor/Telegram 路径。当前已确认纳入的子目标包括：

- 让 Discord voice message 上传绕过 Carbon `files` 自动 multipart 分支，改为显式 JSON `/attachments` 请求
- 统一语音消息转码到 48kHz OGG/Opus，避免部分 TTS/音频源在 Discord 中半速播放
- 为 voice command 与 voice manager 统一复用 owner allowlist/access 解析与 mention formatting
- 为 voice manager 增加 speaker context 缓存，并在转录入站时把 owner 身份带进 agent ingress
- 用新的 `voice-message` 单测和 `voice manager` e2e 覆盖替代旧的 manager unit test

### 当前已纳入范围

- voice upload / codec handling
  - `src/discord/voice-message.ts`
  - `src/discord/voice-message.test.ts`
- voice command / manager
  - `src/discord/voice/command.ts`
  - `src/discord/voice/manager.ts`
  - `src/discord/voice/manager.e2e.test.ts`
  - `src/discord/voice/manager.test.ts` (删除，改由更贴近真实链路的覆盖替代)

### 当前明确排除范围

- `src/discord/monitor/message-handler.*`
- `src/discord/monitor/agent-components.ts`
- `src/telegram/**`

### 当前定向验证

当前这一批已通过的验证包括：

- `pnpm test -- src/discord/voice-message.test.ts`
- `pnpm exec vitest run --config vitest.e2e.config.ts src/discord/voice/manager.e2e.test.ts`

### 当前结论

截至当前，discord voice pipeline 第二十九批已经达到“voice upload/codec、owner gating 与 speaker context 收口清楚、可以进入独立提交整理”的状态。下一步应继续避开 Telegram 大主线，优先处理 Discord monitor 剩余较小支线或 ACP 支线。

## 当前阶段：discord monitor control surface 第三十批改造

### 目标

第三十批当前收口的主题是 Discord monitor 的 gateway wait surface、DM command gating、native command dispatch、auto presence 与 exec approvals 的控制面，而不是更宽的 message-handler/agent-components/Telegram 路径。当前已确认纳入的子目标包括：

- 抽出 Discord DM command access/decision helper，统一 pairing/open/allowlist 下的 slash command 授权逻辑
- 让 native command dispatch 复用新的 DM command helper，并补 ACP binding 不可用等失败路径
- 为 auto presence 增加基于 auth profile 可用性的 presence 状态决策
- 补强 gateway wait surface、presence、exec approvals 和顶层 monitor 回归，确保 monitor control 面不依赖 message-handler 主线

### 当前已纳入范围

- gateway / presence / exec approvals
  - `src/discord/monitor.gateway.ts`
  - `src/discord/monitor.gateway.test.ts`
  - `src/discord/monitor/presence.ts`
  - `src/discord/monitor/presence.test.ts`
  - `src/discord/monitor/exec-approvals.ts`
  - `src/discord/monitor/exec-approvals.test.ts`
  - `src/discord/monitor/auto-presence.ts`
  - `src/discord/monitor/auto-presence.test.ts`
- DM/native command control surface
  - `src/discord/monitor/dm-command-auth.ts`
  - `src/discord/monitor/dm-command-auth.test.ts`
  - `src/discord/monitor/dm-command-decision.ts`
  - `src/discord/monitor/dm-command-decision.test.ts`
  - `src/discord/monitor/native-command.ts`
  - `src/discord/monitor/native-command.model-picker.test.ts`
  - `src/discord/monitor/native-command.options.test.ts`
  - `src/discord/monitor/native-command.plugin-dispatch.test.ts`
- monitor regression coverage
  - `src/discord/monitor.test.ts`
  - `src/discord/monitor/monitor.test.ts`

### 当前明确排除范围

- `src/discord/monitor/message-handler.*`
- `src/discord/monitor/agent-components.ts`
- `src/telegram/**`

### 当前定向验证

当前这一批已通过的验证包括：

- `pnpm test -- src/discord/monitor.gateway.test.ts src/discord/monitor/exec-approvals.test.ts src/discord/monitor/native-command.model-picker.test.ts src/discord/monitor/native-command.options.test.ts src/discord/monitor/native-command.plugin-dispatch.test.ts src/discord/monitor/dm-command-auth.test.ts src/discord/monitor/dm-command-decision.test.ts src/discord/monitor/auto-presence.test.ts src/discord/monitor/presence.test.ts src/discord/monitor.test.ts src/discord/monitor/monitor.test.ts`

### 当前结论

截至当前，discord monitor control surface 第三十批已经达到“gateway/native command/dm command/auto presence/exec approvals 控制面收口清楚、可以进入独立提交整理”的状态。下一步应继续避开 Telegram 大主线，优先处理 Discord message-handler 剩余主线或 ACP 支线。

## 当前阶段：discord message handler and inbound queue 第三十一批改造

### 目标

第三十一批当前收口的主题是 Discord message-handler preflight、inbound job/queue、message utils 与主处理链超时/自发消息过滤，而不是更宽的 agent-components/tool-result/Telegram 路径。当前已确认纳入的子目标包括：

- 抽出 Discord inbound job materialization 与 keyed inbound worker，统一 sessionKey/baseSessionKey 级别的串行队列
- 为 message-handler preflight 增加更完整的上下文准备与 ACP binding 相关校验
- 为主处理链增加 bot-self 过滤、worker timeout、busy/activeRuns 状态回写与更稳定的 message utils 解析
- 为上述 preflight/process/queue/message-utils 行为补齐直接回归

### 当前已纳入范围

- inbound job / worker
  - `src/discord/monitor/inbound-job.ts`
  - `src/discord/monitor/inbound-job.test.ts`
  - `src/discord/monitor/inbound-worker.ts`
- message handler core
  - `src/discord/monitor/message-handler.ts`
  - `src/discord/monitor/message-handler.test-harness.ts`
  - `src/discord/monitor/message-handler.preflight.ts`
  - `src/discord/monitor/message-handler.preflight.types.ts`
  - `src/discord/monitor/message-handler.preflight.test.ts`
  - `src/discord/monitor/message-handler.preflight.acp-bindings.test.ts`
  - `src/discord/monitor/message-handler.process.ts`
  - `src/discord/monitor/message-handler.process.test.ts`
  - `src/discord/monitor/message-handler.inbound-contract.test.ts`
  - `src/discord/monitor/message-handler.bot-self-filter.test.ts`
  - `src/discord/monitor/message-handler.queue.test.ts`
  - `src/discord/monitor/message-utils.ts`
  - `src/discord/monitor/message-utils.test.ts`

### 当前明确排除范围

- `src/discord/monitor/agent-components.ts`
- `src/discord/monitor.tool-result.*`
- `src/telegram/**`

### 当前定向验证

当前这一批已通过的验证包括：

- `pnpm test -- src/discord/monitor/message-handler.preflight.test.ts src/discord/monitor/message-handler.preflight.acp-bindings.test.ts src/discord/monitor/message-handler.process.test.ts src/discord/monitor/message-handler.inbound-contract.test.ts src/discord/monitor/message-handler.bot-self-filter.test.ts src/discord/monitor/message-handler.queue.test.ts src/discord/monitor/message-utils.test.ts src/discord/monitor/inbound-job.test.ts`

### 当前结论

截至当前，discord message handler and inbound queue 第三十一批已经达到“preflight/process/queue/message-utils 主处理链收口清楚、可以进入独立提交整理”的状态。下一步应继续避开 Telegram 大主线，优先处理 Discord agent-components/tool-result 薄层或 ACP 支线。

## 当前阶段：discord agent-components and tool-result 第三十二批改造

### 目标

第三十二批当前收口的主题是 Discord component interaction 与 tool-result 薄层，而不是更宽的 message-handler/Telegram 主线。当前已确认纳入的子目标包括：

- 让 agent component 兼容 Carbon Components v2 的 `cid` 解析，同时保持旧 `componentId` payload 可用
- 为 component 交互补齐 group command authorization、DM allowlist store 读取与 main-session owner pin 行为
- 将共享字面量 `"*"` wildcard registration 改成独立 sentinel customId，避免组件类型之间的注册冲突
- 把 `mentionPatterns` / thread routing / reply-to-bot implicit mention 这条 tool-result 回归迁到 e2e 配置，并为 responsePrefix 断言补足异步等待

### 当前已纳入范围

- component interaction / wildcard registration
  - `src/discord/monitor/agent-components.ts`
  - `src/discord/monitor/agent-components.wildcard.test.ts`
- tool-result dispatch regression
  - `src/discord/monitor.tool-result.test-harness.ts`
  - `src/discord/monitor.tool-result.sends-status-replies-responseprefix.test.ts`
  - `src/discord/monitor.tool-result.accepts-guild-messages-mentionpatterns-match.e2e.test.ts`
  - `src/discord/monitor.tool-result.accepts-guild-messages-mentionpatterns-match.test.ts`（删除，改由 e2e 配置覆盖）

### 当前明确排除范围

- `src/discord/monitor/message-handler.*`
- `src/telegram/**`
- `src/discord/mentions.*`
- `src/discord/monitor/preflight-audio.ts`

### 当前定向验证

当前这一批已通过的验证包括：

- `pnpm test -- src/discord/monitor.tool-result.sends-status-replies-responseprefix.test.ts src/discord/monitor/agent-components.wildcard.test.ts`
- `pnpm exec vitest run --config vitest.e2e.config.ts src/discord/monitor.tool-result.accepts-guild-messages-mentionpatterns-match.e2e.test.ts`

### 当前结论

截至当前，discord agent-components and tool-result 第三十二批已经达到“component registration / authorization 薄层与 tool-result 回归边界清楚、可以进入独立提交整理”的状态。下一步应继续避开 Telegram 大主线，优先处理剩余 Discord 零散文件或转入下一个更大的非 Discord 主题批次。

## 当前阶段：discord mention helpers and preflight audio 第三十三批补齐

### 目标

第三十三批当前收口的主题是补齐已经被 Discord 已提交代码引用、但尚未独立提交的新 helper 文件，避免 clean checkout 时出现缺文件状态。当前已确认纳入的子目标包括：

- 补齐 Discord 已知用户 mention 重写 helper，支持用户/角色/频道 mention 格式化
- 为 outbound/voice 路径提供 account-scoped mention rewrite，跳过 `@everyone`、`@here` 与 markdown code segment
- 补齐 Discord guild audio preflight helper，把“仅音频、无 typed text、且 requireMention”场景的预转写逻辑从 `message-handler.preflight` 主文件中抽出
- 用直接 util 测试和既有 preflight 回归，确认这两个 helper 在当前提交链中闭合

### 当前已纳入范围

- mention helper
  - `src/discord/mentions.ts`
  - `src/discord/mentions.test.ts`
- preflight audio helper
  - `src/discord/monitor/preflight-audio.ts`
  - 依赖回归：`src/discord/monitor/message-handler.preflight.test.ts`

### 当前明确排除范围

- `src/discord/monitor/thread-bindings.ttl.test.ts`
- `src/telegram/**`
- 非 Discord 主线的其它未提交文件

### 当前定向验证

当前这一批已通过的验证包括：

- `pnpm test -- src/discord/mentions.test.ts src/discord/monitor/message-handler.preflight.test.ts`

### 当前结论

截至当前，discord mention helpers and preflight audio 第三十三批已经达到“缺口文件补齐、clean checkout 不再缺引用源码、可以进入独立提交整理”的状态。下一步应处理 Discord 剩余的 thread-binding 测试清理，或者正式转入下一个更大的非 Discord 主题批次。

## 当前阶段：discord thread-binding ttl test cleanup 第三十四批清理

### 目标

第三十四批当前收口的主题是清理已经被新的 thread-binding 生命周期测试吸收的旧 TTL 测试文件，避免 Discord 目录里保留一份与现有测试矩阵重复、且语义已经过时的测试副本。当前已确认纳入的子目标包括：

- 确认旧 `thread-bindings.ttl.test.ts` 中的 TTL、sweeper、rebind、token refresh、persist-unbind 等核心场景均已迁入新的 lifecycle/shared-state/discord-api/persona 测试矩阵
- 删除旧测试文件，避免后续 thread-binding 测试继续双轨维护
- 用当前 thread-binding 测试集合重新验证，确保删除旧文件不会造成覆盖缺口

### 当前已纳入范围

- 删除旧测试
  - `src/discord/monitor/thread-bindings.ttl.test.ts`
- 现有覆盖确认
  - `src/discord/monitor/thread-bindings.lifecycle.test.ts`
  - `src/discord/monitor/thread-bindings.shared-state.test.ts`
  - `src/discord/monitor/thread-bindings.discord-api.test.ts`
  - `src/discord/monitor/thread-bindings.persona.test.ts`

### 当前明确排除范围

- `src/telegram/**`
- 非 Discord 主线的其它未提交文件

### 当前定向验证

当前这一批已通过的验证包括：

- `pnpm test -- src/discord/monitor/thread-bindings.lifecycle.test.ts src/discord/monitor/thread-bindings.shared-state.test.ts src/discord/monitor/thread-bindings.discord-api.test.ts src/discord/monitor/thread-bindings.persona.test.ts`

### 当前结论

截至当前，discord thread-binding ttl test cleanup 第三十四批已经达到“旧 TTL 测试被新测试矩阵完整吸收、可以安全删除遗留副本”的状态。下一步可以正式离开 Discord 主线，转入当前工作区里下一个更大的未提交主题批次。

## 当前阶段：line account and webhook hardening 第三十五批改造

### 目标

第三十五批当前收口的主题是 LINE 账号解析、媒体下载类型探测与 webhook pre-auth/ack 行为加固，而不是更宽的 Telegram/agents/config 主线。当前已确认纳入的子目标包括：

- 为 LINE 多账号配置补齐 `defaultAccount`，并让默认账号解析优先使用显式配置且做标准化匹配
- 让 LINE DM allowlist store 合并 helper 对齐新的 `mergeDmAllowFromSources` 命名
- 修正 LINE 媒体下载中 ISO BMFF `ftyp` 检测，区分常见 M4A 音频与 MP4 视频
- 收紧 Node/Express webhook 的预鉴权 body limit 与 timeout，支持 HEAD 探活，并确保事件处理失败时不提前返回 200
- 让 `send.ts` 支持复用注入的 `cfg`，避免发送路径总是重新 `loadConfig()`

### 当前已纳入范围

- account/config/access
  - `src/line/accounts.ts`
  - `src/line/accounts.test.ts`
  - `src/line/bot-access.ts`
  - `src/line/config-schema.ts`
  - `src/line/types.ts`
- media/send
  - `src/line/download.ts`
  - `src/line/download.test.ts`
  - `src/line/send.ts`
- webhook
  - `src/line/webhook.ts`
  - `src/line/webhook.test.ts`
  - `src/line/webhook-node.ts`
  - `src/line/webhook-node.test.ts`

### 当前明确排除范围

- `src/telegram/**`
- `src/agents/**`
- 非 LINE 主线的其它未提交文件

### 当前定向验证

当前这一批已通过的验证包括：

- `pnpm test -- src/line/accounts.test.ts src/line/download.test.ts src/line/webhook.test.ts src/line/webhook-node.test.ts`
- `pnpm test -- src/line/accounts.test.ts src/line/auto-reply-delivery.test.ts src/line/bot-handlers.test.ts src/line/bot-message-context.test.ts src/line/download.test.ts src/line/flex-templates.test.ts src/line/markdown-to-line.test.ts src/line/monitor.fail-closed.test.ts src/line/monitor.lifecycle.test.ts src/line/monitor.read-body.test.ts src/line/probe.test.ts src/line/reply-chunks.test.ts src/line/rich-menu.test.ts src/line/send.test.ts src/line/template-messages.test.ts src/line/webhook-node.test.ts src/line/webhook.test.ts`

### 当前结论

截至当前，line account and webhook hardening 第三十五批已经达到“默认账号/媒体类型/Webhook 预鉴权与 ACK 语义收口清楚、可以进入独立提交整理”的状态。下一步可继续处理剩余的小 channel 主题，或转入 routing/security/pairing 这类共享底层批次。

## 当前阶段：signal access policy and send config closure 第三十六批补齐

### 目标

第三十六批当前收口的主题是补齐 Signal event handler 已经依赖的 access-policy helper，并把发送路径的配置注入与 MIME kind 解析对齐到共享实现。当前已确认纳入的子目标包括：

- 抽出 Signal DM/group access 决策与 pairing challenge 处理，复用共享 `dm-policy-shared` helper
- 为 Signal allowlist 增加 recipient 规范化 helper，供 main-session owner pin 等共享逻辑使用
- 让 `send.ts` / `send-reactions.ts` 支持复用注入的 `cfg`，避免总是重新 `loadConfig()`
- 为 reaction notification 在 `allowlist` / `pairing` 下的阻断与放行补齐回归测试

### 当前已纳入范围

- access / identity
  - `src/signal/monitor/access-policy.ts`
  - `src/signal/identity.ts`
- send
  - `src/signal/send.ts`
  - `src/signal/send-reactions.ts`
- regression coverage
  - `src/signal/monitor.tool-result.sends-tool-summaries-responseprefix.test.ts`

### 当前明确排除范围

- `src/telegram/**`
- `src/agents/**`
- 非 Signal 主线的其它未提交文件

### 当前定向验证

当前这一批已通过的验证包括：

- `pnpm test -- src/signal/monitor.tool-result.sends-tool-summaries-responseprefix.test.ts src/signal/send-reactions.test.ts src/signal/monitor/event-handler.inbound-contract.test.ts src/signal/monitor/event-handler.mention-gating.test.ts`
- `pnpm test -- src/signal/client.test.ts src/signal/format.chunking.test.ts src/signal/format.links.test.ts src/signal/format.test.ts src/signal/format.visual.test.ts src/signal/identity.test.ts src/signal/monitor.test.ts src/signal/monitor/event-handler.inbound-contract.test.ts src/signal/monitor/event-handler.mention-gating.test.ts src/signal/monitor.tool-result.pairs-uuid-only-senders-uuid-allowlist-entry.test.ts src/signal/monitor.tool-result.sends-tool-summaries-responseprefix.test.ts src/signal/probe.test.ts src/signal/send-reactions.test.ts`

### 当前结论

截至当前，signal access policy and send config closure 第三十六批已经达到“access-policy 缺口补齐、发送配置注入和 reaction allowlist 回归收口清楚、可以进入独立提交整理”的状态。下一步可继续处理剩余的小 channel 主题，如 iMessage / web / WhatsApp，或者转入共享 routing/security/pairing 批次。

## 当前阶段：imessage and web media kind alignment 第三十七批改造

### 目标

第三十七批当前收口的主题是 iMessage 与 web 媒体处理路径对齐共享 `kindFromMime` 语义，并恢复 web 本地媒体 unsafe-bypass 的显式防护，而不是更宽的 commands/config/agents 主线。当前已确认纳入的子目标包括：

- 让 iMessage 发送路径在 MIME 带参数、大小写混杂时仍能正确生成 `<media:audio>` 等占位文本
- 让 iMessage allow target 类型直接复用共享 `ParsedChatTarget`，减少本地重复类型定义
- 让 web 远程/本地媒体加载统一使用 `kindFromMime`
- 恢复 `sandboxValidated` / `localRoots === "any"` 场景下缺少 `readFileOverride` 时的 `unsafe-bypass` 拒绝逻辑

### 当前已纳入范围

- iMessage
  - `src/imessage/send.ts`
  - `src/imessage/send.test.ts`
  - `src/imessage/targets.ts`
- web
  - `src/web/media.ts`
  - 回归验证：`src/web/media.test.ts`
  - 依赖验证：`src/web/outbound.test.ts`

### 当前明确排除范围

- `src/whatsapp/resolve-outbound-target.test.ts`
- `src/telegram/**`
- `src/agents/**`

### 当前定向验证

当前这一批已通过的验证包括：

- `pnpm test -- src/imessage/send.test.ts src/imessage/targets.test.ts src/web/media.test.ts src/web/outbound.test.ts`

### 当前结论

截至当前，imessage and web media kind alignment 第三十七批已经达到“共享 MIME kind 对齐与 web 本地媒体 bypass 防护收口清楚、可以进入独立提交整理”的状态。下一步可单独处理 WhatsApp 测试重排，或转入更大的共享底层批次。

## 当前阶段：whatsapp outbound target test cleanup 第三十八批清理

### 目标

第三十八批当前收口的主题是清理 WhatsApp outbound target 单测中的重复 mock/setup 逻辑，让 allowlist 判定覆盖更集中、更易维护，而不改生产代码行为。当前已确认纳入的子目标包括：

- 抽出 direct-message 目标规范化的共享 test helper
- 抽出 allow/deny 断言 helper，减少各模式下重复样板代码
- 保持 implicit / heartbeat / custom mode / whitespace 场景覆盖不变

### 当前已纳入范围

- `src/whatsapp/resolve-outbound-target.test.ts`

### 当前明确排除范围

- 所有非测试文件
- `src/telegram/**`
- `src/agents/**`

### 当前定向验证

当前这一批已通过的验证包括：

- `pnpm test -- src/whatsapp/resolve-outbound-target.test.ts`

### 当前结论

截至当前，whatsapp outbound target test cleanup 第三十八批已经达到“测试样板代码收口清楚、行为覆盖保持不变、可以进入独立提交整理”的状态。下一步应正式转入剩余更大的共享底层批次，例如 routing/security/pairing/sessions。

## 当前阶段：routing pairing and sessions account-scope closure 第三十九批改造

### 目标

第三十九批当前收口的主题是 routing / pairing / sessions 这一层的 account-scope、默认账号提示和会话事件一致性，而不把更大的 `security/**` 批次混进来。当前已确认纳入的子目标包括：

- 让 pairing request 与 allowFrom 存储在多账号场景下严格按 account 隔离，同时仅对默认账号保留 legacy allowFrom 回退
- 为 allowFrom 读取增加基于文件存在性/mtime/size 的缓存与失效逻辑，并保持 async/sync 路径行为一致
- 抽出共享 `pairing-challenge` helper，统一 DM pairing 模式下的 create-if-missing + reply 流程
- 为 gateway pairing setup 解析补齐 SecretRef/token/password 分支、显式 auth mode 要求与默认端口解析
- 为 routing 默认账号提示、session key/account id 解析和 transcript/model-override 事件补齐辅助逻辑与回归

### 当前已纳入范围

- pairing
  - `src/pairing/pairing-store.ts`
  - `src/pairing/pairing-store.test.ts`
  - `src/pairing/pairing-challenge.ts`
  - `src/pairing/setup-code.ts`
  - `src/pairing/setup-code.test.ts`
- routing
  - `src/routing/account-id.ts`
  - `src/routing/bindings.ts`
  - `src/routing/default-account-warnings.ts`
  - `src/routing/resolve-route.ts`
  - `src/routing/resolve-route.test.ts`
  - `src/routing/session-key.ts`
  - `src/routing/session-key.test.ts`
- sessions
  - `src/sessions/model-overrides.ts`
  - `src/sessions/model-overrides.test.ts`
  - `src/sessions/session-key-utils.ts`
  - `src/sessions/transcript-events.ts`
  - `src/sessions/transcript-events.test.ts`

### 当前明确排除范围

- `src/security/**`
- `src/telegram/**`
- `src/agents/**`

### 当前定向验证

当前这一批已通过的验证包括：

- `pnpm test -- src/pairing/pairing-store.test.ts src/pairing/setup-code.test.ts src/routing/resolve-route.test.ts src/routing/session-key.test.ts src/sessions/model-overrides.test.ts src/sessions/transcript-events.test.ts`
- `pnpm test -- src/pairing/pairing-messages.test.ts src/pairing/pairing-store.test.ts src/pairing/setup-code.test.ts src/routing/account-id.test.ts src/routing/account-lookup.test.ts src/routing/resolve-route.test.ts src/routing/session-key.continuity.test.ts src/routing/session-key.test.ts src/sessions/model-overrides.test.ts src/sessions/send-policy.test.ts src/sessions/transcript-events.test.ts`

### 当前结论

截至当前，routing pairing and sessions account-scope closure 第三十九批已经达到“pairing allowFrom/account scope、setup-code 解析和 session helper 收口清楚、可以进入独立提交整理”的状态。下一步应单独处理 `security/**`，或者再向更大的 `config/commands/agents` 主线推进。

## 当前阶段：security audit deep checks 第四十批改造

### 目标

第四十批当前收口的主题是 `security audit` 主线本身，而不是整个 `security/**` 目录。当前已确认纳入的子目标包括：

- 为 deep audit 引入可复用的 code-safety summary cache，减少插件/技能重复扫描
- 增加 workspace `skills/**/SKILL.md` symlink escape 检查
- 让 gateway probe 在 SecretRef 无法解析时返回明确 warning，而不是静默失败
- 在 audit finding 里补齐 Control UI wildcard allowed origins、Feishu doc create 权限提示，以及更多基于 sourceConfig 的通道审计场景
- 让 audit-time gateway auth/config 判定正确识别 SecretRef、env 和 inferred auth mode

### 当前已纳入范围

- `src/security/audit.ts`
- `src/security/audit-extra.async.ts`
- `src/security/audit-extra.sync.ts`
- `src/security/audit-extra.ts`

### 当前明确排除范围

- `src/security/external-content.ts`
- `src/security/fix.ts`
- `src/security/safe-regex.ts`
- `src/security/skill-scanner.ts`
- `src/security/windows-acl.ts`

### 当前定向验证

当前这一批已通过的验证包括：

- `pnpm test -- src/security/audit.test.ts src/security/audit-extra.sync.test.ts`

### 当前结论

截至当前，security audit deep checks 第四十批已经达到“deep audit 新 finding、SecretRef probe warning、workspace skill symlink escape 和缓存路径收口清楚、可以进入独立提交整理”的状态。下一步继续处理剩余 `security/**` 的扫描/修复/权限批次。

## 当前阶段：security scanner and content guards 第四十一批改造

### 目标

第四十一批当前收口的主题是安全扫描与输入内容判定，不包含自动修复/权限收紧逻辑。当前已确认纳入的子目标包括：

- 提升 safe-regex 对歧义交替与长输入窗口匹配的判定能力
- 强化 skill-scanner 对风险代码模式的静态识别
- 扩展 external-content 对 bracketed internal marker spoof 和更多角括号变体的检测

### 当前已纳入范围

- `src/security/safe-regex.ts`
- `src/security/safe-regex.test.ts`
- `src/security/skill-scanner.ts`
- `src/security/skill-scanner.test.ts`
- `src/security/external-content.ts`
- `src/security/external-content.test.ts`

### 当前明确排除范围

- `src/security/fix.ts`
- `src/security/windows-acl.ts`
- `src/security/temp-path-guard.test.ts`

### 当前定向验证

当前这一批已通过的验证包括：

- `pnpm test -- src/security/safe-regex.test.ts src/security/skill-scanner.test.ts src/security/external-content.test.ts`

### 当前结论

截至当前，security scanner and content guards 第四十一批已经达到“regex/skill 扫描与外部内容 spoof 检测收口清楚、可以进入独立提交整理”的状态。下一步继续处理 `security fix / windows-acl / temp-path-guard` 这条修复与权限批次。
